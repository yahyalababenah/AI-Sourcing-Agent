# AI-Sourcing Hub — All Dependencies

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Database
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0
alembic>=1.13.0
psycopg2-binary>=2.9.9

# Redis & Caching
redis>=5.0.0

# Async task queue
celery>=5.3.6

# Configuration
pydantic[email]>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Auth
pyjwt[crypto]>=2.8.0
bcrypt>=4.1.0
passlib[bcrypt]>=1.7.4

# HTTP client
httpx>=0.26.0

# PDF & Document processing
WeasyPrint>=61.0
jinja2>=3.1.2
pdf2image>=1.17.0
pypdf2>=3.0.0

# Structured-file readers (Excel / CSV / Word tables read directly, no LLM)
pandas>=2.2.0
openpyxl>=3.1.0
python-docx>=1.1.0

# LLM JSON repair
json-repair>=0.25.0

# Object storage
boto3>=1.34.0

# Logging
python-json-logger>=2.0.7

# Monitoring
sentry-sdk[fastapi]>=2.0.0
prometheus-client>=0.19.0

# Rate limiting
slowapi>=0.1.9

# Image processing
pillow>=10.2.0

# Local OCR (replaces external Vision LLM API — OpenRouter/Qwen-VL)
# PaddleOCR for table extraction; text-based PDFs handled via pypdfium2 directly.
# Note: paddlepaddle 3.x with onednn may error on some CPUs; ocr_client.py
# handles this gracefully and falls back to pypdfium2 text extraction.
paddlepaddle>=3.0.0
paddleocr>=3.0.0

# ===== Test dependencies =====
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-timeout>=2.2.0
