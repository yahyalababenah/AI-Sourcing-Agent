# PLAN.md — خطة تنفيذ واجهات AI-Sourcing Hub (v2)

> **لأي جلسة Claude Code جديدة: هذا الملف هو نقطة البداية.**
> اقرأه كاملاً، ثم اقرأ `CLAUDE.md`، ثم نفّذ "بروتوكول بداية الجلسة"
> أدناه. ابدأ من أول مهمة `[ ]` ولا تقفز للأمام أبداً.

---

## بروتوكول بداية كل جلسة (إلزامي)

```bash
# 1. أين نحن؟
git log --oneline -5        # آخر commits
git status                  # هل يوجد شغل غير محفوظ؟

# 2. ماذا أنجزنا؟
# اقرأ "سجل الجلسات" أسفل هذا الملف + أول مهمة [ ] غير مكتملة

# 3. هل المشروع يعمل؟
npm run dev                 # يجب أن يعمل بلا أخطاء قبل البدء بمهمة جديدة
```

- إن وجدت شغلاً غير محفوظ من جلسة سابقة: افحصه. إن كان سليماً
  أكمله واعمل commit. إن كان مكسوراً: `git checkout .` وأعد المهمة.
- إن كان `npm run dev` مكسوراً: إصلاحه هو مهمتك الأولى قبل أي شيء.

## بروتوكول نهاية كل جلسة (إلزامي)

1. commit لكل شغل سليم (لا تترك شغلاً معلقاً بلا commit).
2. علّم المهام المكتملة `[x]` في هذا الملف.
3. أضف سطراً في "سجل الجلسات" أسفل الملف.
4. إن اكتشفت مهمة ناقصة أثناء العمل: أضفها للمرحلة المناسبة بترقيم
   فرعي (مثل T2.3) — لا تنفّذها فوراً إلا إن كانت blocker.

---


> **⚠️ ملاحظة الألوان**: ملفات `handoff-designs/*.html` بُنيت قبل
> استبدال لوحة المستورد. أي indigo/بنفسجي فيها يُقرأ كمقابله من
> عائلة الأزرق البحري الجديدة في `tokens.ts` (نفس الموضع، اللون
> الجديد). الحَكَم النهائي للألوان هو `tokens.ts` وليس الـHTML.

## قواعد العمل الإلزامية

1. **مهمة واحدة في كل مرة.** لا استثناءات.
2. **commit بعد كل مهمة ناجحة**: `feat(plan): T<رقم> — <وصف>`
3. **لا تلمس ما لم يُطلب**: routing، API، صفحات خارج المهمة الحالية.
4. **معيار القبول شرط للتعليم `[x]`**: تحقق فعلياً (متصفح/curl) لا
   نظرياً.
5. **الفشل مرتين = BLOCKED**: اكتب `BLOCKED: <السبب>` تحت المهمة
   وانتقل لمهمة مستقلة. لا تدور بحلقة محاولات.
6. **الحَكَم البصري**: ملفات `handoff-designs/*.html`. عند أي تعارض
   بين وصف نصي والـHTML المرجعي، الـHTML يفوز.
7. **الحَكَم المنطقي**: `CLAUDE.md` (الألوان، القوائم، RFQ-per-View،
   قاعدة الأرقام المالية، نمط الموبايل المزدوج).
8. **RTL دائماً**: `text-start/end`، `ps-*/pe-*`، `ms-*/me-*` بدل
   left/right الثابتة.

---

## المرحلة 0 — الأمان والأساس

- [x] **T0.1 — نقطة رجوع آمنة**
  - checkpoint سليم موجود مسبقاً (`1757e64`). لا تغييرات معلّقة أخرى
    في شجرة العمل (باستثناء `ai-sourcing-hub/` — مجلد فرعي غريب
    مُتتبَّع كـ gitlink منذ commit `f195c2c` الأول، بلا `.gitmodules`؛
    ليس له علاقة بهذه المهمة ولم يُلمس).
  - قبول: `git log` يظهر الـcheckpoint. ✅

- [x] **T0.2 — توكنز الألوان في Tailwind**
  - `tailwind.config.ts` و`fontFamily` كانا موجودين مسبقاً من commit
    سابق، لكن ألوان `importer` كانت لا تزال البنفسجي/indigo المحظور
    (`#4F46E5` وعائلته) في **أربعة مواضع**: `frontend/tailwind.config.ts`،
    `frontend/src/lib/tokens.ts`، `BottomNav.tsx` (ACTIVE_COLOR.client)،
    و`ClientDashboard.tsx` (STATUS_BADGES). تم تصحيحها كلها للأزرق
    البحري الصحيح من `lib/tokens.ts` الجذري (`#1D6FB8` وعائلته).
  - قبول: تحقّق فعلي عبر curl لـCSS المولّد من Vite —
    `.bg-importer-500 { background-color: rgb(29 111 184) }` (=#1D6FB8) و
    `.bg-supplier-500 { background-color: rgb(16 185 129) }` (=#10B981). ✅
  - `npx tsc --noEmit` نظيف بعد التعديل.

- [x] **T0.3 — Cairo + RTL افتراضي**
  - موجود مسبقاً: `<html lang="ar" dir="rtl">` + رابط Google Fonts
    Cairo/Inter/Noto Sans SC في `index.html`. تحقّق عبر curl. ✅

- [x] **T0.4 — تشغيل نظيف + commit**
  - `npm run dev` (Vite) يعمل بلا أخطاء، HMR طبّق التعديلات بلا
    console errors (راجع devlog). ✅

---

## المرحلة 1 — المكوّنات المشتركة

> المرجع: `CLAUDE.md` أقسام "مكوّنات جاهزة" و"بنية الشاشات".

- [x] **T1.1 — `Sidebar.tsx`** — prop `role: 'agent'|'client'|'admin'`،
  قوائم الأدوار الثلاثة من `CLAUDE.md`، `w-64` على اليمين، العنصر
  النشط بلون الدور، بطاقة مستخدم أسفله.
  - كان المكوّن موجوداً من commit سابق ومربوطاً فعلياً في
    `AgentLayout`/`ClientLayout`/`AdminLayout`. وُجد خلل: عنصرا
    "منتجاتي" و"مراجعة المنتجات" (route مختلف) يتشاركان نفس
    `labelKey` (`nav.myProducts`) في قائمة agent — تم فصلهما بمفتاح
    ترجمة جديد `nav.productReview` (أضيف لـar/en/zh).
  - **مهمة فرعية اكتُشفت (غير blocker)**: قائمة admin تفتقد لعنصر
    "إدارة المستخدمين" (لا يوجد route/صفحة بعد) — أُضيفت كـ T9.3
    أدناه بدل تنفيذها الآن (تحتاج بناء صفحة كاملة).
  - قبول: تحقّق عبر `tsc --noEmit` نظيف + Sidebar مستخدم فعلياً في
    الثلاث layouts. ✅
- [x] **T1.2 — `MobileDrawer.tsx`** — نفس المحتوى، ينزلق من اليمين،
  overlay يغلقه، X، وأسفله الإعدادات + تسجيل خروج (أحمر).
  - كان المكوّن موجوداً (overlay + Sidebar bare + logout أحمر) لكن
    ناقص زر **X** للإغلاق — أُضيف (أعلى الدرج، `end-3` بعيداً عن
    شعار Sidebar) + مفتاح ترجمة `common.close` بالثلاث لغات.
  - الانزلاق/الحركة الفعلية (200ms) تُطبَّق في T1.7 حسب تسلسل الخطة.
  - قبول: تحقّق فعلي — 4 اختبارات Vitest جديدة
    (`MobileDrawer.test.tsx`) تغطي: عدم الظهور وهو مغلق، ظهور
    المحتوى/زر الإغلاق/تسجيل الخروج وهو مفتوح، إغلاقه بالنقر على
    الـoverlay وعلى X، واستدعاء `logout()` عند الضغط. 64/64 اختبار
    ناجح بالمشروع كاملاً بعد التعديل. ✅
- [x] **T1.3 — `BottomNav.tsx`** — prop `role`، 5 عناصر كحد أقصى:
  - agent: الرئيسية | الواردة | اللقطات | محادثات | حسابي
  - client: الرئيسية | اكتشف | ريلز | محادثات | حسابي
  - كانت القوائم القديمة مختلفة تماماً عن CLAUDE.md (طلبات الشراء/
    منتجاتي/السوق العالمي بدل الواردة/اللقطات/حسابي، وبلا تبويب
    "حسابي" إطلاقاً). أُعيد بناؤها لتطابق القائمتين حرفياً؛ الأدوات
    والصفحات كلها موجودة مسبقاً (`RFQ.SUPPLIER_INBOX`، `AGENT.REELS`،
    `PROFILE`) **عدا** صفحة ريلز المستورد التي لم تكن موجودة (مؤجلة
    لـT6.3) — أُضيف لها stub بنفس نمط `SettingsPage` (route جديد
    `CLIENT.REELS` + `ClientReelsPage.tsx`) بدل ترك تبويب معطّل، على
    أن يُستبدل بالمشغّل الحقيقي في T6.3.
  - أُصلح أيضاً `left-0 right-0` الثابتة إلى `start-0 end-0` (قاعدة
    RTL في CLAUDE.md).
  - قبول: تحقّق فعلي — اختباري Vitest جديدان (`BottomNav.test.tsx`)
    يتحققان من العناصر الخمسة بالنص العربي الحرفي لكل دور. 66/66
    اختبار ناجح بالمشروع كاملاً. ✅
- [x] **T1.4 — `TopBar.tsx` (موبايل)** — ☰ (يفتح الـDrawer) + عنوان +
  أفاتار.
  - كان المكوّن مكتملاً وصحيحاً بالفعل (☰ يفتح الـdrawer، شعار+عنوان،
    أفاتار بلون مشتق من الاسم + قائمة بروفايل). لم يكن مختبَراً من
    قبل — أضفت `TopBar.test.tsx` (تحقّق فعلي: ظهور العناصر الثلاثة +
    فتح الـdrawer عند الضغط على ☰). 68/68 اختبار ناجح بالمشروع كاملاً.
    ✅
- [x] **T1.5 — مكوّنات ذرّية** — `StatCard`, `LineRow`, `ReelTile`
  (حرفياً من `CLAUDE.md`) + **`StatusPill`** موحّد لكل حالات الطلبات:
  ```
  قيد الانتظار → slate | تحت المراجعة → sky
  جارٍ التفاوض → amber | قيد التنفيذ → لون الدور | مكتمل → emerald
  ```
  - لم تكن موجودة إطلاقاً (بحث شامل: صفر نتائج) — بُنيت من الصفر في
    `src/components/ui/`. `StatusPill` يأخذ `role` prop لتلوين
    "قيد التنفيذ" بلون الدور الحالي (supplier/importer). `ReelTile`
    مبني حرفياً من مواصفة CLAUDE.md — لا يعرض مشاهدات إطلاقاً، فقط
    عدّاد RFQ الأخضر البارز.
  - **مهمة فرعية اكتُشفت (غير blocker)**: `ReelsStudioPage.tsx` لديه
    `ReelTile` محلي خاص به لا يستخدم هذا المكوّن المشترك ولا يعرض
    عدّاد RFQ إطلاقاً (مخالفة لقاعدة RFQ-per-View الذهبية) — أُضيفت
    كملاحظة تحت T6.2 لدمجها عند إعادة بناء أستوديو الريلز.
  - قبول: بدل صفحة اختبار حية داخل الراوتر (لا تخدم CLAUDE.md ولا
    القوائم)، تحقّق فعلي عبر `atoms.test.tsx` — 6 اختبارات Vitest
    تغطي القيم/الألوان الأربعة الثابتة + لون الدور الديناميكي +
    الـoverlay التجاري الدائم + غياب أي عدّاد مشاهدات. 74/74 اختبار
    ناجح بالمشروع كاملاً. ✅
- [x] **T1.6 — `EmptyState.tsx` + `Skeleton.tsx`** — مكوّن فراغ موحّد
  (أيقونة + "لا توجد بيانات" + زر فعل اختياري) وskeleton تحميل بسيط.
  كل قائمة/جدول لاحقاً يستخدمهما — لا نصوص فراغ مخصصة متفرقة.
  - لم يكونا موجودين؛ 8 صفحات موجودة (SupplierRfqInbox،
    MarketplacePage، ClientDashboard، ...) لديها بلوكات "لا توجد
    بيانات" و`animate-pulse` مكرّرة يدوياً بنفس النمط تقريباً. بُني
    `EmptyState`/`Skeleton` في `components/ui/` بنفس نمط الـcard
    الموجود (`card p-12 text-center`) ليطابق الأسلوب الحالي. دمج
    الصفحات الثمانية فيهما هو **T10.3** بالضبط (موجودة أصلاً في
    الخطة) — لم يُنفَّذ هنا تجنباً لتوسيع نطاق T1.6.
  - قبول: تحقّق فعلي — 5 اختبارات Vitest (`EmptyState.test.tsx`,
    `Skeleton.test.tsx`) تغطي العرض الأساسي، إخفاء الزر حين لا يوجد
    action، تفعيل الزر، ودعم className مخصص للـskeleton. 79/79
    اختبار ناجح بالمشروع كاملاً. ✅
- [x] **T1.7 — نظام الحركة الموحّد**
  - طبّق قسم "الحركة والتفاعل" من `CLAUDE.md`: أنشئ classes/أنماط
    موحّدة (زر، بطاقة، عنصر قائمة) بحالات hover/active المحددة،
    وأضف دعم `prefers-reduced-motion`.
  - كل مكوّنات المرحلة 1 (Sidebar، Drawer، BottomNav، الذرّية)
    تستخدم هذه الأنماط — الـDrawer ينزلق 200ms والoverlay يتلاشى.
  - أهم خلل مُصلَح: `MobileDrawer` كان يُركَّب/يُفكَّك بـ
    `if (!drawerOpen) return null` بلا أي انتقال — يظهر/يختفي فجأة
    (مخالف مباشر لقبول T1.7). أُعيد بناؤه ليبقى مثبّتاً دائماً
    ويتحكم بالظهور عبر classes (`translate-x-full`↔`translate-x-0`
    على الدرج، `opacity-0`↔`opacity-100` على الـoverlay) مع
    `transition duration-200`.
  - أُضيف `active:scale-[0.98]` لكل الأزرار التفاعلية التي كانت
    تفتقده (☰ في TopBar، زر البروفايل، زر تسجيل الخروج، جرس
    الإشعارات، X وتسجيل الخروج في الـDrawer، تبويبات BottomNav)
    + `duration-150` على انتقالات الألوان الناقصة.
  - `btn-primary`/`btn-secondary` في `index.css` كانا بلا
    `active:scale-[0.98]` — أُضيف.
  - دعم `prefers-reduced-motion` أُضيف كقاعدة CSS عامة في
    `index.css` (تُصفّر مدة كل transition/animation لمن يطلبه) بدل
    تكرار `motion-reduce:` على كل عنصر.
  - قبول: تحقّق فعلي — 3 اختبارات Vitest محدَّثة في
    `MobileDrawer.test.tsx` تتحقق من classes الإخفاء/الإظهار الفعلية
    (translate/opacity/aria-hidden) بدل مجرد وجود العنصر في DOM، +
    فحص curl لـCSS المولّد يؤكد وجود قاعدة `prefers-reduced-motion`
    و`.translate-x-full` فعلياً. 79/79 اختبار ناجح بالمشروع كاملاً.
    ✅ — **نهاية المرحلة 1**.

---

## المرحلة 2 — صفحة تسجيل الدخول

> هذه من أقوى شاشات المنصة الحالية — عمود الثقة فيها يبيع المنصة
> قبل الدخول. نحسّنها لا نلغيها.

- [x] **T2.1 — الديسكتوب**: عمودان —
  - يمين (RTL: يظهر أولاً): عمود الثقة بخلفية brand-900 متدرجة:
    الشعار + "التوريد الذكي بدقة الذكاء الاصطناعي" + 3 بطاقات
    (دقة 0.82% / نافذة عطاء 48 ساعة / +240 مورّد معتمد) + شارات
    (حماية المشتري، ISO 9001، ترجمة تلقائية).
  - يسار: النموذج — 3 تبويبات دور (مستورد/مورد/الإدارة)، حقول
    البريد وكلمة المرور، زر دخول بلون الدور المختار (importer-500
    للمستورد، supplier-500 للمورد، slate-800 للإدارة)، "نسيت كلمة
    المرور؟"، "إنشاء حساب جديد"، سطر SSL.
  - قبول: تبديل التبويب يغيّر لون الزر فعلياً.
  - **اكتُشفت مخالفتان جوهريتان** لقاعدة CLAUDE.md الرابعة (ملفين
    منفصلين، لا صفحة responsive وحيدة): (1) `LoginPage.tsx` كان
    ملفاً واحداً يبدّل بين تخطيطي ديسكتوب/موبايل بـ
    `hidden lg:flex`/`lg:hidden` — قُسّم إلى `LoginPageDesktop.tsx` +
    `LoginPageMobile.tsx` + مبدّل رفيع `LoginPage.tsx` يعتمد على
    `useMediaQuery` (hook جديد) بدل CSS toggling داخل نفس الملف؛
    منطق النموذج المشترك استُخرج لـ`useLoginForm.ts` لتفادي تكراره.
    (2) زر الدخول كان بلون أخضر ثابت `#059669` بغضّ النظر عن الدور
    المختار — لم يكن يتغيّر إطلاقاً رغم أن هذا هو معيار القبول
    الصريح. أُصلح ليأخذ `importer-500`/`supplier-500`/`slate-800`
    فعلياً حسب `activeRole`. كذلك استُبدل خلفية عمود الثقة
    `#064e3b` (hex يدوي) بـ`bg-brand-900` من tokens.ts.
  - قبول: تحقّق فعلي — `LoginPageDesktop.test.tsx` (3 اختبارات)
    تثبت أن لون الزر يتغيّر فعلياً مع كل تبويب. ✅
- [x] **T2.2 — الموبايل**: عمود الثقة يصير شريطاً مضغوطاً أعلى
  النموذج (الشعار + سطر القيمة + البطاقات الثلاث أفقياً scrollable).
  - نُفّذت مع T2.1 كملف مستقل `LoginPageMobile.tsx` (نفس الانقسام
    أعلاه). أُضيف polyfill لـ`window.matchMedia` في `test/setup.ts`
    (لم يكن موجوداً — jsdom لا يوفره) + `LoginPage.switcher.test.tsx`
    يثبت اختيار الملف الصحيح حسب نتيجة `matchMedia`. جميع اختبارات
    LoginPage القديمة (6) استمرت بالنجاح دون تعديل ضد تخطيط الموبايل
    الجديد. 84/84 اختبار ناجح بالمشروع كاملاً.

---

## المرحلة 3 — رئيسية المندوب/المورد

> المرجع: `supplier-home-desktop.html` / `supplier-home-mobile.html`.

- [x] **T3.1 — الديسكتوب**: Sidebar(agent) + ترحيب/تاريخ + شريط
  "طلبات تنتظر ردّك" (supplier-600، بطاقات واردة، زر أبيض، رسالة
  الضغط الزمني) + 4 KPI + لوحة Kanban بأربعة أعمدة (StatusPill
  للنقاط) + "أستوديو لقطات المصنع" (زر رفع + شبكة، تحت كل مقطع:
  مشاهدات باهتة + "N طلب سعر" supplier-600 بارز).
  - `AgentDashboard.tsx` كان ملفاً واحداً responsive (308 سطر) —
    مخالفة مباشرة لقاعدة CLAUDE.md الرابعة، ونمط CSS-variables قديم
    (`var(--surface)`...) بدل توكنز slate/supplier، وKanban محلي لا
    يستخدم `StatusPill` المشترك، وبلا شريط "تنتظر ردّك" ولا أستوديو
    لقطات إطلاقاً. قُسّم على نمط LoginPage تماماً:
    `AgentDashboard.tsx` (مبدّل رفيع عبر `useMediaQuery`) +
    `AgentDashboardDesktop.tsx` (البناء الفعلي الجديد) +
    `AgentDashboardMobile.tsx` (نقل حرفي للتطبيق القديم، بديل مؤقت
    لحين T3.2 الحقيقية). استُخرج منطق الجلب المشترك (RFQs/إحصاءات/
    عروض مقبولة/منتجات) إلى `useAgentDashboardData.ts` ليشترك فيه
    الملفان بلا تكرار.
  - Kanban الجديد يستخدم `StatusPill` مباشرة لعناوين الأعمدة — حالات
    الطلب (`open/processing/quoted/closed`) تطابق حرفياً تصنيف
    CLAUDE.md (`pending/under_review/negotiating/completed`).
  - قسم الريلز: لا يوجد أي backend لعدّاد RFQ لكل لقطة في المشروع
    كله (تحقّقت من ذلك) — بدل اختلاق أرقام، اتُّبع نفس نهج
    `ReelsStudioPage.tsx` الصادق: يعرض منتجات حقيقية من
    `catalogService` عبر `ReelTile` المشترك مع `rfqCount={0}` صريح
    + تنويه أصفر "رفع الفيديو غير متاح بعد" (نفس نص الصفحة الأخرى).
  - قبول: تحقّق فعلي — `AgentDashboardDesktop.test.tsx` (5 اختبارات:
    KPI، شريط تنتظر ردّك يظهر/يختفي بشرط، Kanban بأعمدة StatusPill
    الأربعة، تنويه الريلز + عدّاد صفر صادق) و
    `AgentDashboard.switcher.test.tsx` (2 اختبار يتحققان من اختيار
    الملف الصحيح حسب `matchMedia`). `npx tsc --noEmit` نظيف،
    91/91 اختبار ناجح بالمشروع كاملاً (تعطّل تشغيل الاختبارات
    بالتوازي الافتراضي بسبب قيود بيئة العمل — أعيد التشغيل بـ
    `--no-file-parallelism` بنجاح). `npm run dev` (منفذ بديل) يعمل
    بلا أخطاء console. ✅
- [x] **T3.2 — الموبايل**: TopBar + 3 KPI + شريط الواردة + Kanban
  أفقي scrollable (بطاقات 150px) + الأستوديو + BottomNav + Drawer.
  - `AgentDashboardMobile.tsx` كان نقلاً حرفياً للتطبيق القديم (CSS-variables
    قديمة، بلا شريط "تنتظر ردّك"، بلا أستوديو ريلز، 4 KPI بدل 3). أُعيد
    بناؤه بالكامل من الصفر مطابقاً للمرجع `supplier-home-mobile.html`
    (تحقّق بصري فعلي عبر Playwright screenshot بما أن الملف HTML مبني بحزمة
    JS مصغّرة يتعذّر قراءتها كنص): ترحيب + شريط "تنتظر ردّك" بخلفية
    `supplier-900` وزر أبيض + 3 KPI (بدل 4، القاعدة الذهبية للموبايل) +
    Kanban أفقي ببطاقات `w-[150px]` تستخدم `StatusPill`/`StatCard`/`ReelTile`
    المشتركة (بدل الأنماط المحلية القديمة) + قسم الأستوديو بنفس نهج
    الديسكتوب الصادق (منتجات كتالوج حقيقية + `rfqCount=0` صريح، لا تلفيق
    لتقييم "4.8" الذي يظهر بالمرجع البصري بلا أي backend). TopBar/BottomNav/
    Drawer موجودة أصلاً عالمياً في `AgentLayout` فبقيت كما هي بلا لمس.
  - عدّلت `AgentDashboard.switcher.test.tsx`: الاختبار القديم كان يفترض
    غياب قسم الريلز بالموبايل (صحيح للنسخة المؤقتة القديمة فقط) — بما أن
    T3.2 تبني قسم ريلز حقيقياً بالموبايل، غيّرت نقطة التفريق بين
    الملفين إلى غياب زر "+ طلب جديد" (حصري للديسكتوب) بدل غياب الريلز.
  - قبول: تحقّق فعلي — `AgentDashboardMobile.test.tsx` جديد (5 اختبارات:
    3 KPI فقط، شريط الواردة يظهر/يختفي بشرط، Kanban بأعمدة StatusPill
    الأربعة، تنويه الريلز + عدّاد صفر صادق) + تعديل
    `AgentDashboard.switcher.test.tsx`. `npx tsc --noEmit` نظيف، 101/101
    اختبار ناجح بالمشروع كاملاً (`--no-file-parallelism`). تحقّق فعلي عبر
    Vite: الملف يترجم بلا أخطاء (`curl` على مسار الموديول يعيد 200 بلا
    أخطاء تحويل) ودورة `npm run dev` نظيفة. ✅

---

## المرحلة 4 — رئيسية المستورد

> المرجع: `importer-home-desktop.html` / `importer-home-mobile.html`.

- [x] **T4.1 — الديسكتوب**: Sidebar(client) + ترحيب + CTA
  "طلب عرض سعر جديد" (importer-500) + 4 KPI + "جديد اليوم من
  المصانع" (ReelTiles بأسعار + أسماء المصانع) + "طلباتي الأخيرة"
  (StatusPill لكل طلب).
  - `ClientDashboard.tsx` كان (كـ`LoginPage`/`AgentDashboard` القديمين)
    ملفاً واحداً responsive بمحتوى مختلف تماماً عن هذه المواصفة/المرجع
    البصري: بطاقة "تكلفة الهبوط" + نموذج إنشاء RFQ سريع مضمّن، بلا 4
    KPI وبلا قسم "جديد اليوم من المصانع" إطلاقاً. قُسّم على نفس نمط
    Login/AgentDashboard: `ClientDashboardDesktop.tsx` (بناء جديد مطابق
    لـ`importer-home-desktop.html` + هذه المواصفة) + `ClientDashboardMobile.tsx`
    (نقل حرفي للمحتوى القديم، بديل مؤقت لحين T4.2) + `ClientDashboard.tsx`
    (مبدّل رفيع عبر `useMediaQuery`). منطق الجلب المشترك استُخرج لـ
    `useClientDashboardData.ts`.
  - الـ4 KPI كلها مُشتقة من بيانات حقيقية (لا أرقام ملفّقة): متوسط زمن
    الرد = معدّل الفرق بالساعات بين إنشاء كل RFQ وأول عرض سعر وصله
    (فقط للطلبات التي فعلاً استلمت عرضاً، وإلا "—")، بانتظار الرد =
    عدد RFQs بحالة `open`، طلبات نشطة = `open`+`processing`، صفقات
    مكتملة هذا الشهر = `closed` بـ`updated_at` ضمن الشهر الحالي.
  - "جديد اليوم من المصانع": نفس نهج `AgentDashboardDesktop` الصادق —
    منتجات كتالوج حقيقية عبر `catalogService.search` مع `ReelTile`
    المشترك (`rfqCount={0}` صريح، لا يوجد backend لعدّاد RFQ لكل
    منتج)، واسم المصنع الحقيقي (`factory_name`/`supplier_name`) تحت كل
    بلاطة.
  - "طلباتي الأخيرة": `StatusPill` بنفس تخطيط حالات RFQ المستخدم في
    Kanban المندوب (`open→pending، processing→under_review،
    quoted→negotiating، closed→completed`) + سعر عرض السعر الأخير
    الحقيقي لكل طلب (map عبر `quotesByRfq`) بدل الكود القديم الذي كان
    يعرض `rfq.client_name` (خطأ نسخ من نسخة المندوب — يساوي اسم
    المستخدم نفسه لكل صفوفه، بلا فائدة) — استُبدل بالكمية الحقيقية من
    `productsMap` (نفس أسلوب Kanban المندوب).
  - قبول: تحقّق فعلي — `npx tsc --noEmit` نظيف، 101/101 اختبار ناجح
    بالمشروع كاملاً (`--no-file-parallelism`)، فحص curl لكل الملفات
    الأربعة عبر Vite dev server يعيد 200 بلا أخطاء تحويل. تعذّر تسجيل
    دخول حي (لا backend/DB في هذه البيئة — بلا docker) فتحقّقت بصرياً
    من المرجع عبر Playwright screenshot لـ`importer-home-desktop.html`
    وقارنته ببنية الصفحة الجديدة سطراً بسطر (ترتيب: ترحيب+CTA، 4 KPI،
    شبكة الريلز بتسمية المصنع تحتها، قائمة الطلبات بالسعر/الحالة). ✅
- [x] **T4.2 — الموبايل**: 3 KPI + نفس المحتوى مكدّساً + التنقل
  المزدوج.
  - `ClientDashboardMobile.tsx` كان نقلاً حرفياً للملف القديم (بطاقة
    تكلفة هبوط + نموذج RFQ سريع مضمّن) — بديل مؤقت من T4.1. أُعيد
    بناؤه بالكامل مطابقاً لـ`importer-home-mobile.html` ونمط
    `AgentDashboardMobile`: ترحيب + زر CTA بعرض كامل + 3 KPI فقط
    (`stats.slice(0, 3)`، إسقاط "صفقات مكتملة هذا الشهر" — القاعدة
    الذهبية للموبايل) + شبكة ريلز `grid-cols-3` (3 منتجات بدل 4) بنفس
    نهج الكتالوج الحقيقي/الصادق + قائمة "طلباتي الأخيرة" بنفس منطق
    الديسكتوب (`StatusPill` + سعر عرض السعر الحقيقي). TopBar/BottomNav/
    Drawer موجودة أصلاً عالمياً في `ClientLayout` فبقيت كما هي بلا لمس.
  - قبول: تحقّق فعلي — `npx tsc --noEmit` نظيف، 101/101 اختبار ناجح
    (`--no-file-parallelism`)، فحص curl عبر Vite dev server لكل من
    `ClientDashboardMobile.tsx`/`ClientDashboard.tsx` يعيد 200 بلا
    أخطاء تحويل من جانب السيرفر. تعذّر تسجيل دخول حي (لا backend/DB في
    هذه البيئة) فتحقّقت بصرياً من المرجع عبر Playwright screenshot
    لـ`importer-home-mobile.html` وقارنته ببنية الصفحة الجديدة. ✅ —
    **نهاية المرحلة 4.**

---

## المرحلة 5 — حاسبة التسعير

> المرجع: `pricing-calculator-*.html`. قاعدة صارمة: slate للأرقام،
> أخضر للإجمالي النهائي فقط، `tabular-nums` دائماً.

- [x] **T5.1 — الديسكتوب**: نموذج "بيانات الشحنة" (FOB، الكمية،
  الشحن/وحدة، التأمين%، الجمارك%، الضريبة%، رسوم الميناء، هامش
  المندوب) + بطاقة "التكلفة الواصلة المتوقعة" (LineRow لكل بند،
  شارة "دقة 0.82%"، إجمالي أخضر كبير، زر "إرسال كعرض سعر للعميل").
  الحساب لحظي بنفس معادلات الـHTML المرجعي (تأمين على FOB+شحن،
  جمارك على القيمة الجمركية، ضريبة على القيمة+الجمارك).
  - **تعارض جوهري اكتُشف واستُشير المستخدم بشأنه**: المرجع البصري
    يمثّل حاسبة بسيطة (منتج واحد، حقول يدوية FOB/شحن/تأمين%/جمارك%/
    ضريبة%/رسوم ميناء/هامش مندوب%، حساب فوري بلا شبكة). لكن
    `/agent/calculator` (`PricingCalcPage.tsx`) كانت بالفعل صفحة
    حقيقية تعمل: اختيار RFQ → تحميل منتجاته الحقيقية → استدعاء محرك
    تسعير حقيقي متصل بالباك إند (`POST /pricing/calculate`) بجداول
    رسوم HS حقيقية، ضريبة قابلة للتخصيص لكل HS، قواعد formula/percentage/
    fixed، غرامات، وإنشاء عرض سعر فعلي — تُستخدم من `QuoteBuilderPage`
    أيضاً. إعادة البناء حرفياً حسب المرجع كانت ستفقد هذا المحرك
    الحقيقي بالكامل. سألت المستخدم صراحة؛ اختار: **"أعد التصميم فقط،
    حافظ على المنطق"** — إعادة تصميم بصري فقط (LineRow لكل بند،
    شارة دقة 0.82%، بطاقة إجمالي خضراء) مع الإبقاء الكامل على تدفق
    RFQ الحقيقي ومحرك الباك إند دون أي فقدان وظيفي.
  - نُفّذ: قسمت `PricingCalcPage.tsx` (ملف واحد responsive قديم —
    نفس مخالفة Login/AgentDashboard/ClientDashboard) إلى
    `PricingCalcPageDesktop.tsx` (تصميم جديد: عمودان — نموذج
    الشحنة/جدول المنتجات على اليمين (عريض)، بطاقة النتيجة `sticky`
    على اليسار (380px) مطابقة للمرجع بصرياً) + `PricingCalcPageMobile.tsx`
    (نقل حرفي للتطبيق القديم كاملاً، بديل مؤقت لحين T5.2) +
    `PricingCalcPage.tsx` (مبدّل رفيع `useMediaQuery`، نفس نمط كل
    الصفحات السابقة). استُخدم `usePricingCalculator.ts` (كان موجوداً
    من جلسة سابقة غير مكتملة — untracked — وطابق تماماً منطق الصفحة
    القديمة، فتم اعتماده كخطاف مشترك).
  - بطاقة النتيجة استُخرجت لمكوّن مشترك `PricingResultCard.tsx`:
    تجميع كل بنود المحرك الحقيقي (FOB محول، شحن، تأمين، جمارك، رسوم
    خدمة 070%، تخليص، ضريبة، بدل 301 لكل شحنة، رسوم قواعد مخصصة،
    خصم الدفع المبكر، عمولة) كـ`LineRow` بدل إخفاء تعقيد المحرك
    الحقيقي إرضاءً لبساطة المرجع (نفس نهج الصدق المتّبع في T3.1/T4.1
    — لا تلفيق ولا حذف بيانات حقيقية). الإجمالي بقي بلون
    `supplier-900` الغامق (أخضر فاتح جداً في الظاهر لكنه فعلياً أخضر
    من عائلة supplier — يطابق قاعدة CLAUDE.md "الأخضر للإجمالي
    النهائي فقط" ومطابق للنمط الأصلي بالكود قبل التعديل). جدول تفاصيل
    المنتجات وقواعد الـHS المطبّقة استُخرجا لـ`PricingDetailBreakdown.tsx`
    (شفافية إضافية لا يقدّمها المرجع البسيط، أُبقيت كملحق تحت البطاقتين).
    جدول تعديل المنتجات استُخرج لـ`ProductsInputTable.tsx` ليُستخدم من
    كلا الملفين لاحقاً بلا تكرار.
  - وسّعت `LineRow`'s `value` prop من `string | number` إلى
    `ReactNode` لدعم لف الأرقام بـ`dir="ltr"` (نمط مستخدم بالفعل
    بكل الصفحات المالية) — توسيع نوع متوافق للخلف، لا يكسر أي استخدام
    قائم (تأكّد عبر `atoms.test.tsx`).
  - قبول: تحقّق فعلي — `PricingCalcPageDesktop.test.tsx` جديد (حالة
    الفراغ قبل اختيار RFQ + بطاقة LineRow/شارة الدقة/الإجمالي الأخضر
    بعد الحساب) و`PricingCalcPage.switcher.test.tsx` جديد (يتحقق من
    اختيار الملف الصحيح حسب `matchMedia`). اختبارات `PricingCalcPage.test.tsx`
    القديمة كلها استمرت بالنجاح دون تعديل (تعمل ضد `PricingCalcPageMobile`
    عبر المبدّل، نسخة مطابقة حرفياً للمنطق القديم). `npx tsc --noEmit`
    نظيف، 105/105 اختبار ناجح (`--no-file-parallelism`)، فحص curl لكل
    الملفات الجديدة عبر Vite dev server يعيد 200 بلا أخطاء تحويل،
    `npm run dev` نظيف بلا console errors. ✅
- [x] **T5.2 — الموبايل**: النموذج فوق النتيجة، BottomNav.
  - `PricingCalcPageMobile.tsx` كان لا يزال النقل الحرفي المؤقت من T5.1
    (نمط قديم CSS-variables + عنوان "١. اختيار طلب عرض السعر" مرقّم +
    بطاقة نتيجة محلية مكرَّرة بدل المكوّنات المشتركة). أُعيد بناؤه
    ليستخدم `usePricingCalculator` + `ProductsInputTable` +
    `PricingResultCard` + `PricingDetailBreakdown` نفسها المستخدمة في
    الديسكتوب (بلا أي تكرار منطق) — نفس محرك التسعير الحقيقي، فقط
    مكدّس بعمود واحد (نموذج الشحنة → المنتجات → بطاقة النتيجة بلا
    `sticky`) بدل عمودي الديسكتوب، مطابقاً للمرجع البصري
    `pricing-calculator-mobile.html` (تحقّق بصري عبر Playwright
    screenshot لأن الملف حزمة JS مصغّرة). TopBar/BottomNav/Drawer
    موجودة أصلاً في `AgentLayout` فبقيت كما هي بلا لمس.
  - عدّلت `PricingCalcPage.switcher.test.tsx`: كلا الملفين صار يعرض
    عنوان "بيانات الشحنة" نفسه الآن، فاستبدلت نقطة التفريق بفحص بنيوي
    (وجود/غياب class عمودي `lg:grid-cols-*` الحصري للديسكتوب) بدل فحص
    عنوان نصي. عدّلت أيضاً اختبار `PricingCalcPage.test.tsx` القديم
    الذي كان يتوقع نص "753.60" منفرداً (كان في العرض القديم) —
    أصبح "753.60 JOD" ضمن `LineRow` الإجمالي المشترك الجديد.
  - قبول: تحقّق فعلي — `PricingCalcPageMobile.test.tsx` جديد (حالة
    الفراغ + بطاقة LineRow/شارة الدقة/الإجمالي بعد الحساب + تأكيد غياب
    الشبكة العمودية الحصرية بالديسكتوب). `npx tsc --noEmit` نظيف،
    107/107 اختبار ناجح بالمشروع كاملاً (`--no-file-parallelism`)، فحص
    curl عبر Vite dev server لملف `PricingCalcPageMobile.tsx` يعيد 200
    بلا أخطاء تحويل، لا `text-left/right` ثابتة بالملف الجديد. ✅
- [x] **T5.3 — ربط `POST /api/v1/pricing/calculate/`** مع fallback
  محلي عند فشل الطلب.
  - كان الربط الحقيقي بـ`POST /pricing/calculate` موجوداً أصلاً (منذ
    T5.1) عبر `usePricingCalculator`/`pricingService.calculate` —
    الناقص فعلياً هو "fallback محلي عند فشل الطلب" فقط. بحثت أولاً في
    الباك إند الحقيقي (`app/modules/pricing/engine.py`) بدل اختلاق
    معادلة: استخرجت الثوابت الحقيقية (`exchange_rate_cny_usd=0.14`,
    `exchange_rate_cny_jod=0.1047`, كثافة الحجم 500كغ/CBM بحد أدنى
    0.1 CBM، أسعار شحن العقبة/جدة/افتراضي 75/60/80 دولار للـCBM،
    تأمين 1%، جمارك عامة احتياطية 5% (بلا جدول HS)، رسوم تخليص ثابتة
    150$، عمولة 3%، ضريبة 16%) وبنيتها في ملف جديد مستقل
    `localPricingFallback.ts` (`calculateLocalFallback`) — تقدير
    تقريبي فقط، بلا جدول رسوم HS ولا خصومات MOQ/دفع مبكر ولا رسوم
    070/301/018 (تحتاج الجدول الحقيقي غير المتوفر أوفلاين).
  - في `usePricingCalculator.ts`: فصلت أخطاء التحقق المحلي (لا RFQ/لا
    ميناء/لا منتجات) بصنف `PricingValidationError` جديد عن أخطاء
    الشبكة/الباك إند الحقيقية — فقط الأخيرة تُفعّل الـfallback
    (`onError` يستدعي `calculateLocalFallback` ويملأ `result` بدلاً
    من إظهار خطأ فقط، فلا ينكسر تدفق الحاسبة عند انقطاع الباك إند).
    استُخرج بناء `productsPayload` لدالة مشتركة `buildProductsPayload`
    لتفادي تكراره بين `mutationFn` و`onError`.
  - أضفت حقل `is_local_fallback?: boolean` لـ`CalculatePriceResponse`
    (اختياري، الباك إند الحقيقي لا يرسله أبداً). `PricingResultCard`
    يعرض شارة كهرمانية "⚠️ تقدير محلي — غير متصل بالخادم" بدل "دقة
    0.82%" ويعطّل زر "إرسال كعرض سعر للعميل" (تعطيل صريح مع tooltip)
    عند التقدير المحلي — إرسال عرض سعر فعلي يحتاج الباك إند بأي حال.
    الديسكتوب والموبايل يستفيدان تلقائياً بلا أي لمس لملفيهما (كل
    المنطق في الخطاف والمكوّن المشترك).
  - قبول: تحقّق فعلي — `localPricingFallback.test.tsx` جديد (5
    اختبارات: العلم `is_local_fallback`، سعر الصرف 0.1047، شحن العقبة
    + إجمالي موجب، الحد الأدنى 0.1 CBM بلا وزن، غياب رسوم HS)، واختبار
    جديد في `PricingCalcPageDesktop.test.tsx` يحاكي فشل
    `pricingService.calculate` ويتحقق من ظهور الشارة الكهرمانية وتعطيل
    زر الإرسال. `npx tsc --noEmit` نظيف، 113/113 اختبار ناجح
    (`--no-file-parallelism`)، فحص curl عبر Vite dev server لكل
    الملفات الجديدة/المعدَّلة يعيد 200 بلا أخطاء تحويل. **نهاية
    المرحلة 5.** ✅

---

## المرحلة 6 — الريلز (الجانبان)

> القاعدة الذهبية: المقياس = RFQ الناتجة. المشاهدات ثانوية باهتة.
> كل تفاعل اجتماعي مُعاد تفسيره تجارياً.

- [x] **T6.1 — أستوديو المورد، موبايل** (المرجع:
  `supplier-reels-mobile.html`): مشغّل عمودي ملء الشاشة + طبقة
  تجارية دائمة (مصنع + توثيق + منتج + سعر + زر طلب عرض) + أزرار
  جانبية (احفظ/اسأل/مشاركة/الأداء) + "N طلب سعر ناتج" أخضر +
  زر "ارفع" + تنقّل بين المقاطع + Drawer/BottomNav.
  - `ReelsStudioPage.tsx` كان ملفاً واحداً (شبكة بلاطات) يُستخدم حرفياً
    لكل من الديسكتوب والموبايل — لا مشغّل ملء شاشة إطلاقاً، مخالفة
    لقاعدة CLAUDE.md الرابعة (لم تكن حتى responsive-hack، بل نفس
    المحتوى تماماً بلا أي تخصص لكل شاشة). قسمته لنفس نمط كل المراحل
    السابقة: `ReelsStudioPageMobile.tsx` (بناء جديد كامل — مشغّل ملء
    شاشة حقيقي) + `ReelsStudioPageDesktop.tsx` (نقل حرفي لشبكة
    البلاطات القديمة، بديل مؤقت لحين T6.2 الحقيقية) + `ReelsStudioPage.tsx`
    (مبدّل رفيع `useMediaQuery`، نفس النمط). استُخرج جلب المنتجات +
    اسم/توثيق المصنع لخطاف مشترك `useReelsStudioData.ts` ليستخدمه
    كلا الملفين لاحقاً بلا تكرار (T6.2 ستستفيد منه أيضاً).
  - تحقّق بصري عبر Playwright screenshot لـ`supplier-reels-mobile.html`
    (حزمة JS مصغّرة، كسابقاتها) لفهم البنية: ترحيب/رفع + عدّاد مؤشر
    (N/إجمالي) + أيقونة تشغيل مركزية + أزرار جانبية دائرية (حفظ/سؤال/
    مشاركة — لا يوجد زر "الأداء" منفصل بصرياً بالمرجع الموبايل؛ لوحة
    الأداء ديسكتوب فقط حسب T6.2) + طبقة سفلية دائمة (اسم المصنع +
    شارة توثيق `ShieldCheck` إن كان `verification_status === "verified"`
    + أفاتار الأحرف الأولى + السعر/اسم المنتج + زر "طلب عرض سعر" +
    صف إحصاءات). التنقّل بين المقاطع: سحب لمسي (`touchstart`/`touchend`
    بعتبة 40px) + سهمَي أعلى/أسفل، وانتقال fade 250ms عبر
    `animate-[totalReveal_250ms_ease-out]` (كيفريم موجود مسبقاً في
    `index.css`، لم يُستخدم بأي مكان — أعيد استخدامه بدل إضافة جديد).
  - **صدق بلا اختلاق بيانات** (نفس نهج T3.1/T4.1/الشبكة القديمة): لا
    يوجد backend لعدّاد RFQ لكل لقطة ولا مشغّل فيديو حقيقي — يُعرض
    "0 طلب سعر" صريحاً + "مشاهدات —" + تنويه كهرمانية "رفع الفيديو
    غير متاح بعد"، بدل تلفيق أرقام كالمرجع (18 طلب/1,240 مشاهدة).
    زر "طلب عرض سعر" يفتح `ROUTES.RFQ.SUPPLIER_INBOX` (المندوب يدير
    طلباته الواردة من هناك — لا معنى لمندوب يطلب عرض سعر لمنتجه هو)،
    "سؤال" يفتح `ROUTES.CHAT.LIST`، "مشاركة" يستخدم `navigator.share`
    مع fallback نسخ الرابط، "حفظ" تبديل حالة محلية فقط (لا API حفظ/
    مفضلة موجود بالمشروع — تبديل واجهة بلا تخزين، بلا ادّعاء ميزة
    غير موجودة).
  - قبول: تحقّق فعلي — `ReelsStudioPageMobile.test.tsx` جديد (4
    اختبارات: حالة الفراغ، عرض أول منتج بعدّاد RFQ صفر صادق + شارة
    توثيق + عدّاد الموضع 1/2، التنقّل للمقطع التالي بالسهم يحدّث
    المنتج والعدّاد 2/2، تبديل زر الحفظ بلا انهيار) و
    `ReelsStudioPage.switcher.test.tsx` جديد (يتحقق من اختيار الملف
    الصحيح حسب `matchMedia` — حقل بحث الديسكتوب مقابل غيابه بالموبايل).
    `npx tsc --noEmit` نظيف، 119/119 اختبار ناجح بالمشروع كاملاً
    (`--no-file-parallelism`)، فحص curl عبر Vite dev server لكل
    الملفات الجديدة/المعدَّلة يعيد 200 بلا أخطاء تحويل، لا
    `text-left/right` ثابتة بالملفات الجديدة. ✅
- [x] **T6.2 — أستوديو المورد، ديسكتوب** (المرجع:
  `supplier-reels-desktop.html`): المشغّل 300px + لوحة أداء
  (مشاهدات/طلبات سعر/تحويل/حفظ) + قائمة اللقطات مرتبة حسب RFQ.
  - استبدلت `ReelsStudioPageDesktop.tsx` (نقل حرفي مؤقت لشبكة
    البلاطات القديمة من T6.1) بالتخطيط الحقيقي: `grid-cols-[300px_1fr]`
    — عمود المشغّل (300px، `sticky`) أولاً = يمين بقاعدة "أول عنصر
    JSX يظهر يميناً بصفحة RTL"، وعمود الإدارة (1fr) ثانياً = يسار،
    مطابقاً تماماً لترتيب `supplier-reels-desktop.html` (تحقّق بصري
    عبر Playwright screenshot سابق من T6.1، أعيد استخدامه). سهما
    أعلى/أسفل تحت المشغّل مباشرة (خارج صندوق الفيديو، لا overlay
    كالموبايل — يطابق المرجع البصري).
  - استُخرج منطق التنقّل/الحفظ/المشاركة/السؤال/الرفع/طلب العرض من
    T6.1 لخطاف مشترك جديد `useReelsPlayer.ts` + مكوّن "chrome" مشترك
    `ReelPlayer.tsx` (الشريط العلوي، الأيقونة المركزية، الأزرار
    الجانبية، الطبقة التجارية السفلية الدائمة) — يستخدمهما كل من
    `ReelsStudioPageMobile.tsx` (أُعيد بناؤه ليستخدم المشترك بدل
    تكرار الكود من T6.1) و`ReelsStudioPageDesktop.tsx`، فلا تكرار
    منطق أو markup بين الملفين رغم اختلاف الحاويات (ملء الشاشة
    بالموبايل مقابل عمود 300px ثابت بالديسكتوب).
  - **T6.2.1 (منفَّذة)**: `ReelTile` المحلي القديم داخل الملف (كان لا
    يعرض عدّاد RFQ إطلاقاً) أُزيل بالكامل — لم يُستبدل بالمكوّن
    المشترك `components/ui/ReelTile.tsx` لأن شكله (بلاطة عمودية
    `aspect-[2/3]`) لا يناسب هذا التخطيط الجديد (صفوف أفقية في قائمة
    "كل اللقطات")؛ بدلاً منه صفوف محلية جديدة تعرض عدّاد RFQ صراحة
    (`0` صادق) لكل لقطة، محققةً هدف القاعدة الذهبية (عدّاد RFQ ظاهر
    دائماً) بشكل مناسب للتخطيط بدل فرض شكل بلاطة غير مناسب.
  - **صدق بلا اختلاق بيانات** (نفس نهج كل مرحلة سابقة): لوحة الأداء
    (4 `StatCard`) تعرض "0" لـ"طلبات سعر"/"حفظ" و"—" لـ"معدل
    التحويل"/"مشاهدات" — لا يوجد أي backend لتحليلات الريلز بالمشروع
    كله (تأكّد سابقاً في T3.1/T4.1/T6.1)، فبدل تلفيق أرقام المرجع
    البصري (18/1.5%/42/1,240) تُعرض القيم الحقيقية (صفر/غير متاح) مع
    تنويه كهرمانية صريح "الأرقام أعلاه صفر/غير متاحة حقيقةً، لا
    تقديرات".
  - قبول: تحقّق فعلي — `ReelsStudioPageDesktop.test.tsx` جديد (3
    اختبارات: حالة الفراغ، لوحة أداء صادقة صفر/شرطة بلا أرقام
    ملفّقة، قائمة "كل اللقطات" تبدّل المشغّل عند اختيار صف مختلف) +
    عدّلت `ReelsStudioPage.switcher.test.tsx` (كان يتحقق من محتوى
    الشبكة القديمة المؤقتة، صار يتحقق من العنوان الحقيقي + قائمة "كل
    اللقطات" الحصرية بالديسكتوب). اختبارات `ReelsStudioPageMobile.test.tsx`
    من T6.1 استمرت بالنجاح دون تعديل رغم إعادة بناء الملف داخلياً
    ليستخدم `useReelsPlayer`/`ReelPlayer` المشتركين (لا تغيير بالسلوك
    الظاهر). `npx tsc --noEmit` نظيف، 122/122 اختبار ناجح بالمشروع
    كاملاً (`--no-file-parallelism`)، فحص curl عبر Vite dev server
    لكل الملفات الجديدة/المعدَّلة يعيد 200 بلا أخطاء تحويل، لا
    `text-left/right` ثابتة. ✅
- [x] **T6.3 — مشغّل المستورد (Consumer)، موبايل + ديسكتوب**:
  نفس المشغّل بصرياً لكن من زاوية الاستهلاك:
  - "إعجاب" → **أضف للقائمة المختصرة** (bookmark بمسمى تجاري)
  - "متابعة المصنع" → اشتراك بتنبيهات منتجاته
  - "سؤال" → يفتح محادثة تقود لـRFQ
  - زر "طلب عرض سعر" دائم الحضور
  - **بلا** لوحة أداء ولا زر رفع (هذه للناشر فقط)
  - يدخل له من تبويب "ريلز" في BottomNav للمستورد.
  - `ClientReelsPage.tsx` كان stub ثابت ("سيتم تنفيذ مشغّل الريلز... لاحقاً")
    منذ T1.3. لا يوجد `importer-reels-*.html` مرجعي في `handoff-designs/` (لا
    ديسكتوب ولا موبايل) — بُني اعتماداً على وصف CLAUDE.md النصي ونفس اللغة
    البصرية لمشغّل المورد (`ReelPlayer`/`ReelsStudioPageMobile/Desktop` من
    T6.1/T6.2)، بألوان `importer-*` بدل `supplier-*` وبلا أي عنصر indigo.
  - قُسّم بنفس نمط كل الشاشات السابقة: `ClientReelsPage.tsx` (مبدّل رفيع
    `useMediaQuery`) + `ClientReelsPageDesktop.tsx` (عمود مشغّل 300px sticky +
    قائمة "لقطات أخرى" للتصفح، بلا لوحة أداء وبلا زر رفع) +
    `ClientReelsPageMobile.tsx` (مشغّل ملء شاشة بسحب لمسي/أسهم، نفس نمط
    T6.1). مكوّنات/خطافات جديدة مخصّصة للمستورد بدل إعادة استخدام مكوّنات
    المورد مباشرة (الأزرار والألوان والمنطق مختلفة جوهرياً): `useClientReelsData.ts`
    (يجلب كتالوج السوق العالمي كاملاً عبر `catalogService.search` — بعكس
    `useReelsStudioData` الذي يقتصر على منتجات المندوب نفسه)، `useClientReelsPlayer.ts`
    (تنقّل + "أضف للقائمة المختصرة" + "متابعة المصنع" + مشاركة + "سؤال"→
    `ROUTES.CHAT.LIST` + "طلب عرض سعر"→`ROUTES.RFQ.CREATE`)، و`ClientReelPlayer.tsx`
    (نفس شكل الطبقة التجارية الدائمة لكن بألوان importer، بدون زر رفع، وبدون
    عدّاد RFQ/مشاهدات — هذا مقياس ناشر لا مستهلك؛ استُبدل بصف MOQ/الفئة عند
    توفره).
  - **صدق بلا اختلاق بيانات**: لا يوجد backend لقوائم مختصرة/متابعة مصانع
    بالمشروع — "أضف للقائمة" و"متابعة المصنع" تبديل واجهة محلي فقط (toast
    تأكيد، بلا تخزين فعلي)، بنفس نهج زر "حفظ" في مشغّل المورد. لا يوجد حقل
    توثيق لكل منتج بالكتالوج (`CatalogProduct` لا يحمل `verification_status`)
    فلم تُعرض شارة توثيق مفبركة لكل لقطة — استُبدلت بسطر عام في عمود الديسكتوب
    يوضّح أن كل حساب مصنع موثّق عبر الأدمن (بدل شارة لكل منتج بلا مصدر بيانات
    حقيقي).
  - قبول: تحقّق فعلي — 4 اختبارات Vitest جديدة (`ClientReelsPageMobile.test.tsx`:
    حالة الفراغ، عرض أول منتج + زر "تابع المصنع" بلا عدّاد RFQ/مشاهدات، التنقّل
    للمقطع التالي، تفعيل "متابَع" بعد الضغط) + 3 اختبارات
    (`ClientReelsPageDesktop.test.tsx`: حالة الفراغ، غياب لوحة الأداء وزر
    الرفع، تبديل المشغّل عبر قائمة "لقطات أخرى") + 2 اختبار
    (`ClientReelsPage.switcher.test.tsx`: التحقق من اختيار الملف الصحيح حسب
    `matchMedia`). `npx tsc --noEmit` نظيف، 131/131 اختبار ناجح بالمشروع
    كاملاً (`--no-file-parallelism`)، فحص curl عبر Vite dev server لكل
    الملفات الجديدة يعيد 200 بلا أخطاء تحويل، لا indigo ولا ألوان
    `supplier-*` بالملفات الجديدة. **نهاية المرحلة 6.** ✅

---

## المرحلة 7 — الملفات الشخصية

> المرجع: `importer-profile.html` / `importer-profile-mobile.html`
> + قسم الريلز في `CLAUDE.md` لملف المورد.

- [x] **T7.1 — ملف المستورد (ديسكتوب + موبايل)**: غلاف importer-900
  + شارة "سجل الاستيراد الكامل" + أفاتار + "مستورد موثّق" +
  3 إحصائيات (صفقات / الميناء المفضّل بلون importer / متوسط الطلب)
  + بيانات الشركة + "محفوظاتي ومتابعاتي" (ReelTiles بشارة محفوظ).
  موبايل: سويتشر segmented متصل (محفوظاتي / طلباتي النشطة).
  - `ROUTES.PROFILE` ("/profile") كان يؤدي لصفحة واحدة قديمة
    (`ProfilePage.tsx`) — نموذج تعديل إعدادات (primary-*/gray-* من الثيم
    القديم) مشترك بين الأدوار الثلاثة، لا علاقة له بمواصفة/مرجع T7.1
    (بلا غلاف، بلا إحصاءات، بلا محفوظات). تحقّقت بصرياً عبر Playwright
    screenshot لـ`importer-profile.html`/`importer-profile-mobile.html`
    (حزمة JS مصغّرة كسابقاتها) لفهم البنية الدقيقة.
  - حوّلت `ProfilePage.tsx` لبوابة حسب الدور: `role === 'client'` يعرض
    الصفحة الجديدة `ClientProfilePage` (مبدّل `useMediaQuery` →
    `ClientProfileDesktop`/`ClientProfileMobile`، نفس نمط كل الشاشات
    السابقة)؛ بقية الأدوار (agent/admin) تستمر بالنموذج القديم كما هو —
    أُعيدت تسميته داخلياً `LegacyProfileForm` بلا أي تعديل سلوكي، لحين
    بناء T7.2 الحقيقية للمورد.
  - خطاف مشترك جديد `useClientProfileData.ts` (يستخدمه كلا ملفي
    Desktop/Mobile بلا تكرار): يجلب RFQs/عروض العميل الحقيقية عبر
    `intakeService`/`quotationService` (نفس خدمات `useClientDashboardData`)
    ليحسب "صفقة مكتملة" (عدّ RFQs بحالة `closed`) و"متوسط قيمة الطلب"
    (متوسط `grand_total` لعروض الأسعار المرتبطة بصفقات مكتملة فعلياً —
    "—" صادقة إن لم توجد بعد، لا رقم ملفّق) + منطق "تعديل الملف" (يعيد
    استخدام نفس عقد `PATCH /auth/me` من النموذج القديم، بحقول
    company_name/preferred_port/contact_number/phone، ويحدّث
    `authStore.setUser` بعد الحفظ). مكوّن `ClientProfileEditForm.tsx`
    مشترك للوحة التعديل بين الملفين.
  - **قرار بيانات بلا مصدر حقيقي**: `ClientProfile` (النوع) لا يحمل حقل
    `verification_status` (بعكس `SupplierProfile` الذي له مسار توثيق
    حقيقي عبر الأدمن — T9.3). شارة "مستورد موثّق" أُبقيت ثابتة لكل حساب
    مستورد مسجّل — بخلاف حالات "حفظ/متابعة" (تبديل واجهة محلي بحت، صريح
    وغير مخزَّن، بنفس نهج زر "حفظ" بالريلز) — لأن فلسفة CLAUDE.md نفسها
    تنص أن "كل حساب يمثّل عملاً حقيقياً" ولا يوجد حالة "مستورد غير موثّق"
    مقابلة بنموذج البيانات أصلاً (بعكس المورد الذي له `pending/rejected`
    فعلية)؛ لذا لا يوجد ادّعاء كاذب هنا بعكس لو اخترعت رقم أداء أو بيانات
    محفوظة وهمية. قسم "محفوظاتي ومتابعاتي" بلا نظام حفظ حقيقي بالمشروع
    (تأكّد سابقاً في T6.3) — عُرض `EmptyState` صادق بدل بلاطات ReelTile
    مزيّفة بشارة "محفوظ" لعناصر لم يحفظها المستخدم فعلياً.
  - قبول: تحقّق فعلي — 3 اختبارات (`ClientProfileDesktop.test.tsx`):
    اسم الشركة/شارة التوثيق/الإحصاءات الصادقة المشتقّة من RFQs/عروض
    حقيقية، حالة الفراغ الصادقة للمحفوظات، فتح لوحة التعديل. 2 اختبار
    (`ClientProfileMobile.test.tsx`): تبويب "محفوظاتي" الافتراضي بحالة
    فراغ صادقة، تبويب "طلباتي النشطة" يعرض RFQ حقيقياً مفتوحاً بـ
    `StatusPill`. 2 اختبار (`ClientProfilePage.switcher.test.tsx`) +
    2 اختبار (`ProfilePage.test.tsx`) يتحقق أن بوابة الدور تعرض الصفحة
    الجديدة للمستورد والنموذج القديم للمندوب دون تغيير. `npx tsc --noEmit`
    نظيف، 140/140 اختبار ناجح بالمشروع كاملاً (`--no-file-parallelism`)،
    فحص curl عبر Vite dev server لكل الملفات الجديدة/المعدَّلة يعيد 200
    بلا أخطاء تحويل، لا `text-left/right` ثابتة. ✅
- [x] **T7.2 — ملف المورد (ديسكتوب + موبايل)**: ألوان supplier +
  غلاف بشارة "شاهد جولة داخل المصنع" (زر تشغيل) + "مورد موثّق" +
  إحصائيات (128 صفقة / دقة 0.82% أخضر / 4h زمن الرد) + بيانات
  المصنع + ISO 9001 + سويتشر (المنتجات / لقطات المصنع) + شبكة
  بأسعار overlay دائمة وزر "طلب عرض" لكل عنصر.
  - لا يوجد `supplier-profile-*.html` مرجعي بـ`handoff-designs/` (نفس فجوة
    T6.3) — بُني اعتماداً على نص CLAUDE.md/PLAN.md، بنفس اللغة البصرية
    لـ`ClientProfileDesktop/Mobile` (T7.1) لكن بتوكنز `supplier-*`.
    استكملت بوابة `ProfilePage.tsx` الدور: `role === 'agent'` يعرض
    `SupplierProfilePage` الجديدة (Desktop/Mobile/switcher بنفس النمط)؛
    admin وحده تبقّى على `LegacyProfileForm` (لا مواصفة/مرجع بصري له).
  - خطاف مشترك جديد `useSupplierProfileData.ts` (يستخدمه كلا ملفي
    Desktop/Mobile): يعيد استخدام `useReelsStudioData` (T6.1) نفسه لجلب
    منتجات الكتالوج الحقيقية + اسم المصنع + حالة التوثيق — بلا تكرار
    الجلب — ويضيف فوقه إحصاءات مشتقّة من RFQs/عروض حقيقية عبر
    `intakeService`/`quotationService` (نفس خدمات `useAgentDashboardData`،
    الباك إند يُرجع تلقائياً طلبات/عروض المندوب الحالي بلا فلتر يدوي):
    "صفقة مكتملة" = عدّ RFQs بحالة `closed`، "متوسط زمن الرد" = متوسط
    الفرق بالساعات بين إنشاء كل RFQ وأول عرض قدّمه هذا المورد عليه (نفس
    منطق `avgResponseHours` بلوحة العميل، معكوساً لجهة المورد) — "—"
    صادقة إن لم يوجد عرض بعد. زر "تعديل الملف" يعيد استخدام عقد
    `PATCH /auth/me` (حقول factory_name/location_in_china/specialty/
    factory_address/phone) عبر مكوّن مشترك جديد `SupplierProfileEditForm.tsx`.
  - "دقة 0.82%" **ليست إحصاءة لكل مورد** — نفس الشارة الثابتة على مستوى
    المنصة المستخدمة في `PricingResultCard` (T5.1)، أُعيدت هنا كرقم أخضر
    (`emerald-600`) بدل بادج صغير، مطابقةً لنص الخطة الصريح "دقة 0.82%
    أخضر" — ليست ادّعاءً جديداً بل نفس الرقم المُعلن مسبقاً بمكان آخر
    بالمنصة.
  - **"ISO 9001" بلا حقل شهادة حقيقي لكل مصنع** بنموذج البيانات
    (`SupplierProfile` لا يحمل مثل هذا الحقل) — عُرضت كشارة ثابتة على
    مستوى المنصة، بنفس نهج شاشة تسجيل الدخول (`CLAUDE.md`/T2.1: عمود
    الثقة يعرض "ISO 9001" كشارة منصّة عامة، ليست ادّعاء توثيق لكل حساب
    فردي) — لا فبركة بيانات جديدة، امتداد لسابقة موجودة فعلاً بالمنصة.
    شارة "مورد موثّق" (بعكس ISO 9001) **مشروطة فعلياً** بـ
    `profile.verification_status === 'verified'` الحقيقي (حقل حقيقي
    بمسار توثيق حقيقي عبر الأدمن — T9.3)، فتختفي لموردين `pending`.
  - قسم "المنتجات" (تبويب): بلاطة جديدة مشتركة `SupplierProductTile.tsx`
    (overlay سعر دائم + زر "طلب عرض" منفصل صريح لكل عنصر، بعكس
    `ReelTile` المشترك الذي لا يملك زر عمل منفصل) — الزر يفتح
    `ROUTES.RFQ.SUPPLIER_INBOX` (نفس منطق T6.1: لا معنى لمندوب يطلب عرض
    سعر لمنتجه هو، فالزر يديره لصندوق طلباته الواردة الحقيقي). تبويب
    "لقطات المصنع" يعيد استخدام `ReelTile` المشترك بـ`rfqCount={0}`
    الصادق (نفس نهج T6.1/T6.2 — لا backend لتحليلات الريلز)، والنقر
    ينقل لأستوديو اللقطات الحقيقي (`ROUTES.AGENT.REELS`) حيث يوجد
    التنويه الكهرمانية الصادق أصلاً. شارة "شاهد جولة داخل المصنع" بالغلاف
    تنقل لنفس الأستوديو (لا فيديو حقيقي بعد — الصفحة المقصودة تُفصح عن
    ذلك بنفسها، فلا ادّعاء جديد هنا).
  - قبول: تحقّق فعلي — 3 اختبارات (`SupplierProfileDesktop.test.tsx`):
    اسم المصنع/شارتا التوثيق وISO/دقة 0.82% وعدّ الصفقات المكتملة
    الصادق، تبويب "المنتجات" الافتراضي بزر "طلب عرض" لكل بلاطة، تبويب
    "لقطات المصنع" بعدّاد RFQ صفر صادق. 2 اختبار
    (`SupplierProfileMobile.test.tsx`): إخفاء شارة التوثيق فعلياً لمورد
    `pending` مع بقاء ISO 9001 وشرطة صادقة لزمن الرد (بلا عروض بعد)، فتح
    لوحة التعديل. 2 اختبار (`SupplierProfilePage.switcher.test.tsx`) +
    تحديث `ProfilePage.test.tsx` (3 اختبارات: العميل→الواجهة الجديدة،
    المندوب→الواجهة الجديدة الآن بدل القديم، الأدمن وحده يبقى على
    النموذج القديم). `npx tsc --noEmit` نظيف، 148/148 اختبار ناجح
    بالمشروع كاملاً (`--no-file-parallelism`)، فحص curl عبر Vite dev
    server لكل الملفات الجديدة/المعدَّلة يعيد 200 بلا أخطاء تحويل، لا
    `text-left/right` ثابتة. **نهاية المرحلة 7.** ✅

---

## المرحلة 8 — صفحات سير العمل (Workflows)

> لا مراجع HTML — استنبط من المكوّنات والقواعد. التزم بلون الدور.

- [x] **T8.1 — إنشاء RFQ (client)**: نموذج (اسم المنتج، المواصفات،
  الكمية، صور اختيارية) + معاينة التكلفة التقديرية جانبه (يستدعي
  الحاسبة) + زر إرسال importer-500.
  - `RFQCreatePage.tsx` (`/rfq/create`) كان ملفاً واحداً بلا انقسام Desktop/
    Mobile (سابق لتعميم القاعدة) بحقول حرة فقط (`client_request_arabic`
    نص حر) بلا اسم منتج/مواصفات/كمية منفصلة، بلا معاينة تكلفة، وبزر
    `bg-primary-600` (ثيم قديم لا `importer-500`) — مستخدَم من زر
    "طلب عرض سعر جديد" بلوحتي العميل (Desktop/Mobile) بالفعل (رابط حي، لا
    مسار مكسور).
  - **تعارض جوهري مع عقد الباك إند**: `RFQCreate` (`src/types/intake.ts`)
    يقبل فقط وصفاً نصياً حراً (`client_request_arabic`) — خط أنابيب الذكاء
    الاصطناعي يستخرج المنتجات لاحقاً من النص، لا يوجد حقل منتج/كمية منفصل
    وقت إنشاء الـRFQ. الحل المتّبع (بلا تعديل باك إند): الحقول المهيكلة
    الجديدة (اسم المنتج/المواصفات/الكمية/السعر) تُركَّب في نص
    `client_request_arabic` قابل للقراءة + تُحفَظ كاملة أيضاً بصيغتها
    الخام داخل `extracted_entities` (الحقل الحر الموجود أصلاً بالنوع) —
    لا فقدان بيانات ولا تلفيق حقل باك إند غير موجود.
  - **معاينة التكلفة**: لا يمكن استدعاء محرك التسعير الحقيقي
    (`pricingService.calculate`) لأنه يتطلب `rfq_id` لمنتج محفوظ مسبقاً —
    لا يوجد بعد بهذه المرحلة من التدفق. استُخدم بدلاً `calculateLocalFallback`
    (نفس محرك التقدير المحلي من T5.3، يعمل بلا RFQ محفوظ) مع بند واحد
    مبني من الكمية + السعر المستهدف بالوحدة (يوان صيني، حقل جديد اختياري)
    — يظهر شارة كهرمانية "⚠️ تقدير تقريبي أولي" دائماً (ليس مشروطاً بفشل
    شبكة كسابقتها بالحاسبة، بل صادق بطبيعته: لا RFQ حقيقي بعد). مكوّن
    مشترك جديد `RFQEstimatePreview.tsx` (`LineRow` لكل بند + إجمالي
    `brand-*` أخضر حسب قاعدة CLAUDE.md المالية العامة، لا `importer-*`
    للأرقام) يُستخدم من كلا ملفي Desktop/Mobile.
  - **الصور**: لا يوجد أي endpoint لرفع صور منتج/RFQ بالمشروع كله (يوجد
    فقط `POST /documents/upload` لمستندات RFQ مثل PDF/Excel، منطق مختلف)
    — حقل الصور تبديل واجهة محلي صادق (معاينة أسماء الملفات فقط، تنويه
    كهرمانية صريح أن الرفع غير متصل بخادم)، بنفس نهج "حفظ" بالريلز/زر
    الرفع بلوحة المندوب.
  - قُسّم الملف: `RFQCreatePage.tsx` صار بوابة دور (نفس نمط `ProfilePage.tsx`):
    `role === 'client'` → مبدّل `useMediaQuery` بين `RFQCreatePageDesktop.tsx`/
    `RFQCreatePageMobile.tsx` الجديدين (زر الإرسال `bg-importer-500`،
    حقول تركّز `importer-500`)؛ بقية الأدوار (agent/admin) → النموذج
    القديم كما هو تماماً (أعيدت تسميته داخلياً `LegacyRFQCreateForm`، بلا
    تعديل سلوكي — يُستخدم أيضاً من مسار "قدّم عرضاً" المستقبلي بـT8.2).
    خطاف مشترك جديد `useClientRfqCreate.ts` (منطق النموذج/المعاينة/الإرسال)
    يستخدمه كلا ملفي Desktop/Mobile بلا تكرار.
  - قبول: تحقّق فعلي — عدّلت `RFQCreatePage.test.tsx` (أزلت اختبار الدور
    "client" القديم الذي صار يختبر واجهة مختلفة تماماً الآن، بقية
    الاختبارات — كلها بدور افتراضي فارغ يسقط لنفس النموذج القديم — استمرت
    بالنجاح دون تعديل). 3 اختبارات جديدة (`RFQCreatePage.switcher.test.tsx`):
    عميل/ديسكتوب يعرض النموذج الجديد، عميل/موبايل نفس الشيء بعمود واحد،
    دور غير عميل يبقى على النموذج القديم. 4 اختبارات
    (`RFQCreatePageDesktop.test.tsx`): حالة الفراغ الصادقة قبل إدخال
    الكمية/السعر، ظهور المعاينة بعد إدخالهما، رسالة التحقق عند نقص اسم
    المنتج، الإرسال الفعلي بنص مركّب + `extracted_entities` يحويان
    البيانات المهيكلة. 3 اختبارات (`RFQCreatePageMobile.test.tsx`): تكديس
    عمودي واحد، ظهور المعاينة، تحقق الحقول المطلوبة. `npx tsc --noEmit`
    نظيف، 157/157 اختبار ناجح بالمشروع كاملاً (`--no-file-parallelism`)،
    فحص curl عبر Vite dev server لكل الملفات الجديدة/المعدَّلة يعيد 200
    بلا أخطاء تحويل، لا `text-left/right` ثابتة ولا indigo بالملفات
    الجديدة. لا يوجد backend/DB بهذه البيئة فتعذّر تسجيل دخول حي لتحقق
    بصري متصفح فعلي؛ التحقق اعتمد على الاختبارات + tsc + curl (نفس قيد
    كل المراحل السابقة بهذه البيئة). ✅
- [x] **T8.2 — طلبات العملاء الواردة (agent)**: قائمة RFQs الواردة
  مع عدّاد الزمن منذ الوصول (الضغط الزمني) + زر "قدّم عرضاً" لكل
  طلب يفتح الحاسبة مسبقة التعبئة.
  - `SupplierRfqInbox.tsx` (`/rfq/supplier-inbox`، مربوط أصلاً بزر "طلب
    عرض سعر" من ريلز المندوب T6.1/T6.2/T7.2) كان بالفعل صفحة حقيقية
    وظيفية (710 سطر) — تبويبان "مباريات حصرية" (`intakeService.listMatched`
    + عدّاد تنازلي لمهلة الرد + قبول/رفض) و"السوق العام"
    (`intakeService.listPublic`) — لا مجرد stub. لكنها مخالفة صريحة
    لقاعدة الملفين (ملف واحد responsive بشبكة `grid-cols-1 md:grid-cols-2
    xl:grid-cols-3`) وبثيم قديم (`primary-*`/`gray-*`) بدل `supplier-*`
    الخاص بالمندوب، وبلا "عدّاد زمن منذ الوصول" إطلاقاً (فقط تاريخ ثابت
    `toLocaleDateString`).
  - قسمتها لنفس نمط كل المراحل: `useSupplierRfqInboxData.ts` (خطاف مشترك
    جديد يجمع كل منطق الجلب/المطابقة الدفعية/mutation القبول-الرفض من
    الملف القديم بلا تكرار، + `now` نابض كل 60 ثانية لعدّادات الزمن) +
    `SupplierInboxCards.tsx` (مكوّنات عرض مشتركة: `MatchCard`,
    `PublicRfqCard`, `CountdownTimer`, `ElapsedTimeBadge` الجديد — كلها
    مُعاد تلوينها بتوكنز `supplier-*`/`slate-*` بدل الثيم القديم) +
    `SupplierRfqInboxDesktop.tsx` (شبكة بطاقات + تبويبات كاملة النص +
    `EmptyState`/`Skeleton` المشتركين بدل الكتل اليدوية القديمة) +
    `SupplierRfqInboxMobile.tsx` (عمود واحد مكدّس + تبويبات مضغوطة نصياً
    "الحصرية" بعرض كامل، بلا عنوان فرعي طويل) + `SupplierRfqInbox.tsx`
    (مبدّل رفيع `useMediaQuery`، نفس نمط كل الصفحات).
  - **عدّاد الزمن منذ الوصول** (`ElapsedTimeBadge`، مكوّن جديد): يحسب من
    `rfq.created_at` — "الآن" / "منذ N د" / "منذ N س" / "منذ N ي" / تاريخ
    بعد شهر، بتلوين حسب الضغط الزمني (slate لأقل من ساعة، كهرماني لأقل
    من يوم، أحمر لأكثر من يوم — "كل ساعة تأخير تقلل فرصة الفوز بالصفقة"،
    نفس روح شريط "تنتظر ردّك" بلوحة المندوب T3.1). يستبدل عرض التاريخ
    الثابت القديم بكل من `MatchCard`/`PublicRfqCard`.
  - **قرار "زر قدّم عرضاً يفتح الحاسبة مسبقة التعبئة"**: الزر يفتح
    `ROUTES.RFQ.BUILD_QUOTE(id)` (`QuoteBuilderPage`) كما كان — لم يُغيَّر
    لـ`ROUTES.PRICING.CALCULATE + "?rfq_id="` (الحاسبة العامة، التي
    تدعم فعلياً `rfq_id` كـquery param جاهز بـ`usePricingCalculator`).
    السبب: `QuoteBuilderPage` بانٍ عروض أسعار مخصص لكل RFQ (منطق تحميل/
    تسعير/إنشاء عرض مستقل خاص به) وهي بالفعل "الحاسبة مسبقة التعبئة"
    بمعناها العملي لهذا الطلب تحديداً — التحويل للحاسبة العامة كان
    سيُفقد ميزات بانٍ العروض المخصص (نفس نهج T5.1: لا تفقد منطقاً حقيقياً
    إرضاءً لصياغة حرفية).
  - أُبقي فرق تبويبَي "مباريات حصرية"/"سوق عام" كما هو (وظيفة حقيقية
    موجودة أصلاً، لا مبرر لحذفها لمطابقة صياغة الخطة المبسّطة "قائمة
    RFQs الواردة" — نفس مبدأ عدم فقدان منطق حقيقي من T5.1/T8.1).
  - قبول: تحقّق فعلي — 2 اختبار (`SupplierRfqInbox.switcher.test.tsx`):
    الديسكتوب يعرض تبويب "المباريات الحصرية" كامل النص، الموبايل يعرض
    "الحصرية" المضغوط. 3 اختبارات (`SupplierRfqInboxDesktop.test.tsx`):
    حالة فراغ صادقة، عدّاد الزمن "منذ 2 ي" لمباراة عمرها يومان + قبولها
    فعلياً عبر `claimMatch`، تبويب السوق العام + زر "تقديم عرض سعر فوري".
    2 اختبار (`SupplierRfqInboxMobile.test.tsx`): تكديس عمودي بتبويبات
    مضغوطة، تبديل لتبويب السوق العام بحالة فراغ صادقة. `npx tsc --noEmit`
    نظيف، 164/164 اختبار ناجح بالمشروع كاملاً (`--no-file-parallelism`)،
    فحص curl عبر Vite dev server لكل الملفات الجديدة/المعدَّلة يعيد 200
    بلا أخطاء تحويل، لا `text-left/right` ولا `primary-*`/`gray-*`/indigo
    متبقية بالملفات الجديدة. لا يوجد backend/DB بهذه البيئة فتعذّر تسجيل
    دخول حي. ✅
- [x] **T8.3 — عروض الأسعار (agent)**: جدول العروض المرسلة
  بحالاتها (StatusPill) + قيمها + تاريخها.
  - `QuotationListPage.tsx` (`/quotes`، بلا `RoleGuard` — كل الأدوار)
    كان ملفاً واحداً بجدول حقيقي وظيفي (رقم العرض/العميل/المبلغ/الحالة/
    تتبع الشحنة/التاريخ/إجراءات) لكن غير مقسّم Desktop/Mobile، بثيم قديم
    `primary-*`/`gray-*`، وبشارات حالة يدوية (`STATUS_COLORS`/
    `STATUS_LABELS` محلية) بدل `StatusPill` المشترك كما يطلب النص صراحة.
  - **فجوة حقيقية في `StatusPill`**: حالات العرض الفعلية (`draft, pending,
    finalized, sent, accepted, rejected`) لا تملك مقابلاً لـ"مرفوض" في
    `OrderStatus` الحالي (`pending|under_review|negotiating|in_progress|
    completed`). أضفت قيمة `rejected` جديدة للنوع + `LABELS`/`FIXED_CLASSES`
    (أحمر) — إضافة توافقية للخلف بلا كسر أي استخدام قائم (تحقّق: كل
    اختبارات `atoms.test.tsx` القديمة استمرت بالنجاح + اختبار جديد
    للحالة المضافة). باقي التخطيط: `draft/pending→pending`،
    `finalized→under_review` (جاهز، لم يُرسَل بعد)، `sent→negotiating`
    (بانتظار قرار العميل)، `accepted→completed`، `rejected→rejected`.
  - قسمت الملف بنفس نمط ProfilePage.tsx/RFQCreatePage.tsx:
    `useQuotationListData.ts` (خطاف مشترك: الجلب عبر `quotationService.list`
    + دالتا `quoteStatusPill`/`quoteTrackingStatus` + `TRACKING_LABELS`) +
    `QuotationListPageDesktop.tsx` (جدول مُعاد تلوينه بـ`supplier-*`/
    `slate-*` + `StatusPill` + `EmptyState`/`Skeleton` المشتركين بدل
    الكتل اليدوية، وأُزيلت `text-right` الثابتة من الجدول) +
    `QuotationListPageMobile.tsx` (بطاقات مكدّسة عمودياً بدل جدول عريض
    لا يناسب الموبايل) + `QuotationListPage.tsx` (بوابة دور: `role ===
    'agent'` → مبدّل `useMediaQuery` بين الجديدين؛ بقية الأدوار →
    `LegacyQuotationList` الجدول القديم كما هو تماماً، بلا تعديل سلوكي —
    لحين T8.4 التي ستبني واجهة المستورد على نفس الراوت).
    `(q as any).tracking_status` أُبقي كما كان (نفس النمط المستخدم أصلاً
    بـ`QuotationDetailPage.tsx`/`OrdersListPage.tsx` — الحقل حقيقي من
    الباك إند لكن غير مُعرَّف بنوع `Quotation`؛ تصحيح النوع خارج نطاق
    T8.3 ويلمس ملفات أخرى لم تُطلب).
  - قبول: تحقّق فعلي — 3 اختبارات (`QuotationListPage.switcher.test.tsx`):
    مندوب/ديسكتوب يعرض زر تحديث نصي (الجدول القديم بلا زر تحديث إطلاقاً،
    فهو المائز الحقيقي)، مندوب/موبايل زر تحديث أيقوني، دور غير مندوب يبقى
    على الجدول القديم بلا زر تحديث. 4 اختبارات
    (`QuotationListPageDesktop.test.tsx`): حالة فراغ صادقة، عرض رقم/عميل/
    مبلغ/`StatusPill` لحالة "sent"→"جارٍ التفاوض"، زر "🚚 تتبع" يظهر فقط
    للعروض المقبولة، فتح تفاصيل عرض. 2 اختبار
    (`QuotationListPageMobile.test.tsx`): حالة فراغ، بطاقة بحالة "مكتمل"
    + زر تتبع للعرض المقبول. `npx tsc --noEmit` نظيف، 173/173 اختبار
    ناجح بالمشروع كاملاً (`--no-file-parallelism`)، فحص curl عبر Vite
    dev server لكل الملفات الجديدة/المعدَّلة يعيد 200 بلا أخطاء تحويل،
    لا `primary-*`/`gray-*`/indigo ولا `text-left/right` ثابتة بالملفات
    الجديدة (الجدول القديم `LegacyQuotationList` يحتفظ بـ`text-right`
    القديمة عمداً — كود قديم غير مُلمَّس، نفس نهج `LegacyProfileForm`/
    `LegacyRFQCreateForm`). ✅
- [ ] **T8.4 — طلباتي (client)**: نفس الجدول من زاوية المستورد.
- [ ] **T8.5 — السوق العالمي (client + agent)**: شبكة بطاقات
  (صورة/سعر/مصنع/توثيق/زر طلب عرض) + فلاتر (فئة، بلد المنشأ،
  نطاق سعر) + بحث.
- [ ] **T8.6 — المحادثات**: قائمة غرف + نافذة رسائل، فقاعات
  المستخدم الحالي بلون دوره، زر مرفق "أرسل عرض سعر" داخل المحادثة
  (يربط الشات بالـRFQ — كل محادثة تقود لصفقة).
- [ ] **T8.7 — تتبّع الشحنات**: خط زمني عمودي (تم الشحن → في البحر
  → وصل الميناء → التخليص → التسليم) بنقاط ملوّنة وحالة نشطة.
  - commit بعد كل صفحة.

---

## المرحلة 9 — صفحات الأدمن

> slate بحت، كثافة معلومات عالية، صفر طابع اجتماعي.

- [ ] **T9.1 — لوحة التحكم**: KPIs (طلبات/عروض/مستخدمين/منتجات) +
  تكاليف الذكاء الاصطناعي + إدارة سريعة + قواعد التسعير النشطة.
- [ ] **T9.2 — مراقبة النظام**: حالة الخدمات (DB/Redis/MinIO/
  Celery/LLM) بمؤشرات ملوّنة + زمن استجابة endpoints بأشرطة +
  معلومات البيئة. (المرجع الوظيفي: لقطة المنصة الحالية — حافظ على
  نفس المعلومات بتنسيق النظام الجديد.)
- [ ] **T9.3 — إدارة المستخدمين + توثيق الموردين**: جداول بفلاتر +
  أزرار اعتماد/رفض التوثيق.
  - **T9.3.1** (اكتُشفت في T1.1): أضف route `ADMIN.USERS` + عنصر
    "إدارة المستخدمين" في `Sidebar.tsx` (ADMIN_NAV) بعد بناء الصفحة —
    مفتاح الترجمة `nav.userManagement` موجود مسبقاً بالثلاث لغات.
- [ ] **T9.4 — قواعد التسعير + جداول رسوم HS + الكتالوج العالمي**:
  جداول CRUD بسيطة وواضحة.
  - commit بعد كل صفحة.

---

## المرحلة 10 — ربط الـAPI الشامل

- [ ] **T10.1 — `lib/api.ts` موحّد**: fetch wrapper + baseURL من
  `NEXT_PUBLIC_API_URL` + حقن توكن المصادقة + معالجة أخطاء موحّدة.
- [ ] **T10.2 — استبدال البيانات الوهمية صفحةً صفحة** بالـendpoints
  في `CLAUDE.md` (rfqs / pricing / catalog / auth/me / chat /
  admin). **قاعدة: صفحة واحدة = commit واحد.**
- [ ] **T10.3 — حالات التحميل والفراغ**: `Skeleton` أثناء الجلب،
  `EmptyState` عند لا بيانات، رسالة خطأ + زر "إعادة المحاولة" عند
  الفشل — في كل قائمة/جدول بلا استثناء.

---

## المرحلة 11 — مميزات إضافية

- [ ] **T11.1 — جرس الإشعارات**: أيقونة بعدّاد في الشريط العلوي +
  قائمة منسدلة (RFQ جديد للمورد / عرض جديد للمستورد / تحديث حالة
  شحنة). polling بسيط الآن — لا websockets في هذه المرحلة.
- [ ] **T11.2 — مبدّل اللغة**: في السايد بار والـDrawer (🌐 عربي |
  EN | 中文) — يبدّل locale واتجاه `dir` والخط (Cairo/Inter/Noto).
- [ ] **T11.3 — i18n كامل**: next-intl، ملفات ترجمة ar/en/zh لكل
  النصوص الثابتة. المحتوى المُدخل من المستخدم يُعرض كما هو.
- [ ] **T11.4 — تحسينات لمسية للموبايل**: مساحات لمس ≥44px لكل
  الأزرار، سحب أفقي سلس للـKanban والريلز.

---

## المرحلة 12 — التنظيف والفحص النهائي

- [ ] **T12.1 — إزالة الثيم القديم**: الآن فقط أزل palette القديمة
  والـdark-default وأي CSS ميت. تأكد ألا صفحة انكسرت.
- [ ] **T12.2 — فحص شامل صفحة-صفحة**: الأدوار الثلاثة × ديسكتوب
  وموبايل × مقارنة مع `handoff-designs`. أي انحراف → مهمة فرعية
  جديدة هنا.
- [ ] **T12.3 — فحص القواعد الست**: (1) لا text-left/right ثابتة
  (2) لا hex يدوية خارج tokens (3) لا ألوان دور مخلوطة (4) الأرقام
  المالية slate+أخضر فقط (5) كل قائمة لها Empty/Skeleton (6) كل
  ريل يعرض RFQ لا مشاهدات كمقياس أول. استخدم grep للتحقق:
  ```bash
  grep -rn "text-left\|text-right" app/ components/ | grep -v node_modules
  grep -rn "#[0-9A-Fa-f]\{6\}" app/ components/ | grep -v tokens
  grep -rn "4F46E5\|4338CA\|6366F1\|312E81" . --include="*.ts*" # يجب أن يعيد صفر نتائج (indigo محظور)
  ```
- [ ] **T12.4 — commit نهائي + وسم**: `git tag v1-design-migration`.

---

## سجل الجلسات (حدّثه نهاية كل جلسة)

| التاريخ | المهام المنجزة | ملاحظات / BLOCKED |
|---------|----------------|---------------------|
| 2026-07-05 | T0.1, T0.2, T0.3, T0.4 | T0.1/T0.3/T0.4 كانت مستوفاة مسبقاً (تحقّق فقط). T0.2 كشفت indigo محظور متبقٍ في 4 ملفات (tailwind.config.ts، lib/tokens.ts، BottomNav.tsx، ClientDashboard.tsx) — تم تصحيحها للأزرق البحري. مجلد `ai-sourcing-hub/` الفرعي (gitlink غريب، 1.1GB) لوحظ لكن لم يُلمس — خارج نطاق الخطة. |
| 2026-07-05 | T1.1 | Sidebar.tsx كان موجوداً ومربوطاً بالفعل في الثلاث layouts؛ أصلحت خلل تكرار labelKey بين "منتجاتي" و"مراجعة المنتجات". أضفت T9.3.1 كمهمة فرعية لعنصر admin الناقص "إدارة المستخدمين". |
| 2026-07-05 | T1.2 | MobileDrawer.tsx كان شبه مكتمل؛ أضفت زر X المفقود + مفتاح ترجمة close، وكتبت 4 اختبارات Vitest تغطي فتح/إغلاق/logout (تحقق فعلي بدل نظري). |
| 2026-07-05 | T1.3 | BottomNav.tsx كان يستخدم قوائم مختلفة تماماً عن CLAUDE.md وبلا تبويب "حسابي". أعدت بناءه ليطابق القائمتين حرفياً، وأضفت stub لصفحة ريلز المستورد (route/page جديدين) لأن T6.3 الحقيقية لم تُبنَ بعد. |
| 2026-07-05 | T1.4 | TopBar.tsx كان مكتملاً وصحيحاً مسبقاً بلا اختبارات — أضفت TopBar.test.tsx للتحقق الفعلي. |
| 2026-07-05 | T1.5 | بنيت StatCard/LineRow/StatusPill/ReelTile من الصفر في components/ui/ (لم تكن موجودة). أضفت T6.2.1 لدمج ReelTile المشترك في ReelsStudioPage لاحقاً بدل نسخته المحلية الناقصة لعدّاد RFQ. |
| 2026-07-05 | T1.6 | بنيت EmptyState/Skeleton من الصفر (8 صفحات لديها نسخ يدوية مكررة بنفس النمط تقريباً). دمج الصفحات القائمة مؤجّل لـT10.3 كما هو مخطط أصلاً. |
| 2026-07-05 | T1.7 | أهم إصلاح: MobileDrawer كان يظهر/يختفي فجأة بلا انتقال (return null) — أعدت بناءه بانزلاق CSS فعلي 200ms. أضفت active:scale-[0.98] لكل الأزرار الناقصة له، وقاعدة prefers-reduced-motion عامة. نهاية المرحلة 1 — كل مهام T1.x مكتملة. |
| 2026-07-05 | T2.1, T2.2 | LoginPage.tsx كان ملفاً واحداً responsive (مخالفة صريحة لقاعدة الملفين) وزر الدخول لم يكن يتغيّر لونه بالدور إطلاقاً رغم أنه معيار القبول. قسمتها لـLoginPageDesktop/Mobile.tsx + useLoginForm.ts المشترك + useMediaQuery hook جديد. **ملاحظة مهمة للمراحل القادمة**: لم أتحقق بعد إن كانت صفحات أخرى موجودة (AgentDashboard وغيرها) تعاني نفس مخالفة "ملف responsive وحيد" — يجب فحص كل صفحة عند الوصول لمهامها (T3+) والانقسام إن لزم، بنفس نمط useLoginForm/useMediaQuery. |
| 2026-07-05 | T3.1 | تأكدت الملاحظة السابقة: AgentDashboard.tsx كان نفس مخالفة LoginPage (ملف واحد responsive + ثيم CSS-variables قديم)، قسمته لنفس نمط Desktop/Mobile/switcher + useAgentDashboardData.ts مشترك. Kanban صار يستخدم StatusPill المشترك (تطابق حرفي بين حالات RFQ وتصنيف CLAUDE.md). قسم الريلز الجديد صادق: لا يوجد backend لعدّاد RFQ لكل لقطة بالمشروع كله، فاستُخدم نفس نهج ReelsStudioPage (منتجات كتالوج حقيقية + rfqCount=0 + تنويه أصفر) بدل اختلاق أرقام. AgentDashboardMobile.tsx حالياً نقل حرفي للتطبيق القديم فقط (بديل مؤقت) — إعادة بنائه الفعلية هي T3.2. 91/91 اختبار ناجح (تعطّل التوازي الافتراضي بسبب بيئة العمل — نجح بـ`--no-file-parallelism`)، tsc نظيف، dev server يعمل بلا أخطاء. |
| 2026-07-05 | T3.2 | أعدت بناء AgentDashboardMobile.tsx بالكامل مطابقاً لـsupplier-home-mobile.html (تحقّق بصري عبر Playwright screenshot لأن ملف الـHTML المرجعي حزمة JS مصغّرة). صار يستخدم StatusPill/StatCard/ReelTile المشتركة بدل الأنماط المحلية، 3 KPI فقط (لا 4)، شريط "تنتظر ردّك" بخلفية supplier-900، ونفس نهج الريلز الصادق (rfqCount=0 حقيقي، لا تلفيق لتقييم "4.8" الذي بالمرجع). عدّلت AgentDashboard.switcher.test.tsx لأن نقطة تفريقه القديمة (غياب قسم الريلز بالموبايل) لم تعد صحيحة بعد بناء الريلز الحقيقي — بدّلتها لغياب زر "+ طلب جديد" الحصري بالديسكتوب. 101/101 اختبار ناجح، tsc نظيف، Vite يترجم الملف بلا أخطاء. **نهاية المرحلة 3.** |
| 2026-07-05 | T4.1 | ClientDashboard.tsx كان نفس مخالفة LoginPage/AgentDashboard (ملف واحد responsive) لكن بمحتوى مختلف كلياً عن المرجع/المواصفة (بطاقة تكلفة هبوط + نموذج RFQ سريع، بلا 4 KPI ولا قسم "جديد اليوم من المصانع"). قسمته لنفس نمط Desktop/Mobile/switcher + useClientDashboardData.ts مشترك. الـ4 KPI والريلز وقائمة الطلبات كلها بيانات حقيقية مشتقة (لا أرقام ملفّقة) — راجع تفاصيل الحساب تحت T4.1 أعلاه. ClientDashboardMobile.tsx حالياً نقل حرفي للملف القديم (بديل مؤقت) — إعادة بنائه الفعلية هي T4.2. لا يوجد backend/DB في هذه البيئة (بلا docker) فتعذّر تسجيل دخول حي؛ تحقّق بصري عبر Playwright screenshot لـimporter-home-desktop/mobile.html + فحص curl لتحويل Vite بلا أخطاء. 101/101 اختبار ناجح، tsc نظيف. |
| 2026-07-05 | T4.2 | أعدت بناء ClientDashboardMobile.tsx بالكامل مطابقاً لـimporter-home-mobile.html (نفس نهج T3.2 لـAgentDashboardMobile): 3 KPI فقط (لا 4)، شبكة ريلز grid-cols-3، قائمة طلبات بنفس منطق الديسكتوب. استبدل النقل الحرفي المؤقت من T4.1. **نهاية المرحلة 4.** لا يوجد backend/DB بهذه البيئة فتحقّق بصري عبر Playwright screenshot + فحص curl لتحويل Vite. 101/101 اختبار ناجح، tsc نظيف. |
| 2026-07-05 | T5.1 | اكتُشف تعارض جوهري بين المرجع البصري (حاسبة بسيطة منتج واحد بحقول يدوية) والصفحة الحقيقية الموجودة `/agent/calculator` (تدفق RFQ متعدد المنتجات + محرك تسعير حقيقي بالباك إند: HS codes/VAT/formula rules/إنشاء عرض سعر فعلي، تستخدمه QuoteBuilderPage أيضاً). استشرت المستخدم — اختار الحفاظ على المنطق الحقيقي وإعادة التصميم البصري فقط (تفاصيل القرار والتنفيذ تحت T5.1 أعلاه). قسمت PricingCalcPage.tsx (ملف responsive قديم) لـDesktop/Mobile/switcher بنفس نمط كل الصفحات السابقة، مع مكوّنات مشتركة جديدة (PricingResultCard، ProductsInputTable، PricingDetailBreakdown) ووسّعت LineRow.value لـReactNode. PricingCalcPageMobile.tsx حالياً نقل حرفي مؤقت (بديل لحين T5.2). 105/105 اختبار ناجح (`--no-file-parallelism`)، tsc نظيف، dev server نظيف. |
| 2026-07-05 | T5.2 | استبدلت النقل الحرفي المؤقت من T5.1 بإعادة بناء PricingCalcPageMobile.tsx فعلياً: نفس usePricingCalculator/ProductsInputTable/PricingResultCard/PricingDetailBreakdown المستخدمة بالديسكتوب (بلا تكرار منطق)، مكدّسة بعمود واحد (نموذج→منتجات→نتيجة، بلا sticky) مطابقة لـpricing-calculator-mobile.html (تحقّق بصري Playwright). عدّلت switcher test ليفرّق ببنية العمودين (lg:grid-cols) بدل نص العنوان لأن كلا الملفين صار يشترك بعنوان "بيانات الشحنة" نفسه بعد التوحيد البصري. **نهاية المرحلة 5 المتعلقة بالتصميم — T5.3 (ربط الـAPI) متبقية.** 107/107 اختبار ناجح، tsc نظيف، curl عبر Vite يعيد 200. |
| 2026-07-05 | T5.3 | الربط الحقيقي بـ`POST /pricing/calculate` كان موجوداً أصلاً منذ T5.1 — الناقص كان fallback محلي فقط. بحثت أولاً في محرك الباك إند الحقيقي `app/modules/pricing/engine.py` (عبر Explore agent) لاستخراج الثوابت الحقيقية بدل اختلاق معادلة (أسعار الصرف 0.14/0.1047، كثافة CBM، أسعار شحن العقبة/جدة، تأمين 1%، جمارك عامة 5%، تخليص 150$، عمولة 3%، ضريبة 16%). بنيتها في `localPricingFallback.ts` مستقل، وفصلت أخطاء التحقق المحلي (`PricingValidationError`) عن أخطاء الشبكة الحقيقية في `usePricingCalculator` — فقط الأخيرة تُفعّل الـfallback. أضفت `is_local_fallback` لنوع الاستجابة + شارة كهرمانية وتعطيل زر إرسال العرض في `PricingResultCard` عند التقدير المحلي (إرسال عرض فعلي يحتاج الباك إند بأي حال). **نهاية المرحلة 5 كاملةً.** 113/113 اختبار ناجح (`--no-file-parallelism`)، tsc نظيف، curl عبر Vite يعيد 200 لكل الملفات الجديدة/المعدَّلة. |
| 2026-07-05 | T6.1 | ReelsStudioPage.tsx كان ملفاً واحداً (شبكة بلاطات) مطابقاً حرفياً على الديسكتوب والموبايل معاً — لا مشغّل ملء شاشة إطلاقاً، ومخالفة لقاعدة الملفين. قسمته لنفس نمط كل المراحل: ReelsStudioPageMobile.tsx (بناء جديد كامل — مشغّل عمودي ملء شاشة حقيقي، سحب لمسي + أسهم للتنقّل بين المقاطع، طبقة تجارية سفلية دائمة) + ReelsStudioPageDesktop.tsx (نقل حرفي لشبكة البلاطات القديمة، بديل مؤقت لحين T6.2) + مبدّل + useReelsStudioData.ts مشترك. تحقّق بصري عبر Playwright screenshot لـsupplier-reels-mobile.html. نفس نهج الصدق بلا اختلاق بيانات (0 طلب سعر صريح، لا مشغّل فيديو حقيقي، تنويه كهرمانية)؛ "طلب عرض سعر" يفتح RFQ.SUPPLIER_INBOX (لا معنى لمندوب يطلب عرض سعر لمنتجه)، "حفظ" تبديل واجهة محلي بلا API (لا يوجد بالمشروع). 119/119 اختبار ناجح، tsc نظيف، curl عبر Vite يعيد 200. |
| 2026-07-05 | T6.2 | استبدلت ReelsStudioPageDesktop.tsx (نقل حرفي مؤقت من T6.1) بالتخطيط الحقيقي: grid-cols-[300px_1fr] (مشغّل يمين sticky، إدارة يسار) مطابق لـsupplier-reels-desktop.html. استخرجت منطق التنقّل/الحفظ/المشاركة/السؤال/الرفع من T6.1 لخطاف مشترك جديد useReelsPlayer.ts + مكوّن "chrome" مشترك ReelPlayer.tsx يستخدمهما كل من Mobile وDesktop معاً (أعدت بناء ReelsStudioPageMobile.tsx داخلياً ليستخدمهما بلا تغيير بالسلوك الظاهر — كل اختباراته القديمة استمرت بالنجاح). T6.2.1: أزلت ReelTile المحلي القديم كاملاً (كان لا يعرض RFQ إطلاقاً) بدل استبداله بمكوّن الشبكة المشترك، لأن شكل البلاطة العمودية لا يناسب صفوف قائمة "كل اللقطات" الأفقية الجديدة — صفوف محلية جديدة تعرض عدّاد RFQ صراحة بدلاً. لوحة الأداء تعرض 0/— صادقة (لا backend لتحليلات الريلز) بدل أرقام المرجع الملفّقة (18/1.5%/42/1,240)، مع تنويه كهرمانية صريح. **نهاية T6.1+T6.2 — T6.3 (مشغّل المستورد) متبقية لإنهاء المرحلة 6.** 122/122 اختبار ناجح، tsc نظيف، curl عبر Vite يعيد 200. |
| 2026-07-05 | T6.3 | ClientReelsPage.tsx كان stub ثابت منذ T1.3 بلا أي مشغّل فعلي. لا يوجد importer-reels-*.html مرجعي بالمشروع كله — بُني اعتماداً على نص CLAUDE.md ونفس اللغة البصرية لمشغّل المورد (T6.1/T6.2) لكن بألوان importer ومنطق أزرار مُعاد تفسيره بالكامل من زاوية الاستهلاك: "حفظ"→أضف للقائمة المختصرة، "متابعة المصنع" جديد (زر أعلى بدل زر الرفع)، "سؤال"→المحادثات، "طلب عرض سعر"→ROUTES.RFQ.CREATE. مكوّنات/خطافات مستقلة جديدة (useClientReelsData يجلب كامل كتالوج السوق العالمي لا منتجات مستخدم واحد، useClientReelsPlayer، ClientReelPlayer) بدل إعادة استخدام مكوّنات المورد مباشرة لاختلاف الألوان/الأزرار/المنطق جوهرياً. بلا لوحة أداء وبلا زر رفع (حصريان للناشر) وبلا شارة توثيق مفبركة لكل منتج (CatalogProduct لا يحمل verification_status). **نهاية المرحلة 6 كاملةً.** 131/131 اختبار ناجح (`--no-file-parallelism`)، tsc نظيف، curl عبر Vite يعيد 200 لكل الملفات الجديدة، لا indigo ولا ألوان supplier-* بالملفات الجديدة. |
| 2026-07-05 | T7.1 | ROUTES.PROFILE كان يؤدي لنموذج تعديل إعدادات قديم واحد (primary-*/gray-*) مشترك بين الأدوار، لا علاقة له بمرجع importer-profile*.html (تحقّق بصري Playwright). حوّلت ProfilePage.tsx لبوابة دور: client → ClientProfilePage جديدة (Desktop/Mobile/switcher بنفس نمط كل الشاشات)، بقية الأدوار → النموذج القديم كما هو (أعيدت تسميته LegacyProfileForm داخلياً، بلا تعديل سلوكي، لحين T7.2). useClientProfileData.ts (خطاف مشترك جديد) يحسب "صفقة مكتملة"/"متوسط قيمة الطلب" من RFQs/عروض حقيقية (نفس خدمات useClientDashboardData) — "—" صادقة إن لم توجد عروض مرتبطة بصفقات مكتملة، ويعيد استخدام عقد PATCH /auth/me القديم لزر "تعديل الملف" (مكوّن ClientProfileEditForm مشترك). قرار بيانات: ClientProfile لا يحمل verification_status (بعكس SupplierProfile) فأبقيت شارة "مستورد موثّق" ثابتة لكل حساب مسجّل (لا حالة "غير موثّق" مقابلة بنموذج البيانات، بخلاف المورد) بينما قسم "محفوظاتي ومتابعاتي" يعرض EmptyState صادق (لا نظام حفظ حقيقي بالمشروع، تأكّد سابقاً في T6.3) بدل بلاطات محفوظة مزيّفة. 140/140 اختبار ناجح (`--no-file-parallelism`)، tsc نظيف، curl عبر Vite يعيد 200. |
| 2026-07-05 | T7.2 | لا يوجد supplier-profile-*.html مرجعي (نفس فجوة T6.3) — بُني بنفس لغة ClientProfileDesktop/Mobile (T7.1) بتوكنز supplier-*. أكملت بوابة ProfilePage.tsx: role==='agent' يعرض SupplierProfilePage الجديدة الآن (بدل LegacyProfileForm)، admin وحده يبقى على النموذج القديم. useSupplierProfileData.ts يعيد استخدام useReelsStudioData (T6.1) لجلب المنتجات/الهوية بلا تكرار، ويضيف إحصاءات حقيقية: "صفقة مكتملة" (عدّ RFQs closed) و"متوسط زمن الرد" (فرق ساعات بين إنشاء RFQ وأول عرض قدّمه هذا المورد، نفس منطق لوحة العميل معكوساً). "دقة 0.82%" أُعيدت كرقم أخضر (نفس الشارة الثابتة من PricingResultCard T5.1، ليست إحصاءة لكل مورد) و"ISO 9001" شارة منصّة ثابتة (بنفس سابقة شاشة الدخول T2.1) — بعكسهما "مورد موثّق" مشروط فعلياً بـverification_status الحقيقي فيختفي للموردين pending (تحقّق باختبار). تبويب "المنتجات" يستخدم SupplierProductTile جديد (زر "طلب عرض" صريح لكل بلاطة → RFQ.SUPPLIER_INBOX، نفس منطق T6.1) وتبويب "لقطات المصنع" يعيد استخدام ReelTile المشترك بـrfqCount=0 صادق. **نهاية المرحلة 7 كاملةً.** 148/148 اختبار ناجح (`--no-file-parallelism`)، tsc نظيف، curl عبر Vite يعيد 200 لكل الملفات الجديدة. |
| 2026-07-05 | T8.1 | بدأت المرحلة 8. RFQCreatePage.tsx (`/rfq/create`، مربوط أصلاً بزر "طلب عرض سعر جديد" بلوحة العميل) كان ملفاً واحداً غير مقسّم بحقل وصف حر فقط، بلا حقول منتج/كمية منفصلة وبلا معاينة تكلفة. اكتشفت تعارضاً مع عقد الباك إند: RFQCreate يقبل نصاً حراً فقط — حللته بتركيب الحقول المهيكلة الجديدة في client_request_arabic + حفظها كاملة أيضاً بـextracted_entities (بلا تعديل باك إند ولا فقدان بيانات). معاينة التكلفة تستخدم calculateLocalFallback (نفس محرك T5.3 المحلي) لأن RFQ لم يُحفظ بعد فلا يمكن استدعاء pricingService.calculate الحقيقي — شارة كهرمانية دائمة "تقدير تقريبي أولي" (صادقة بطبيعتها لا بشرط فشل شبكة). حقل الصور تبديل واجهة صادق بلا endpoint حقيقي (لا يوجد رفع صور RFQ بالمشروع). قسمت الملف بنفس نمط ProfilePage.tsx: role==='client' → RFQCreatePageDesktop/Mobile الجديدين (زر importer-500) عبر useClientRfqCreate.ts المشترك؛ بقية الأدوار → LegacyRFQCreateForm (النموذج القديم، بلا تغيير). 157/157 اختبار ناجح (`--no-file-parallelism`، 10 اختبارات جديدة، اختبار دور العميل القديم أُزيل لأنه يختبر واجهة مختلفة الآن)، tsc نظيف، curl عبر Vite يعيد 200 لكل الملفات. لا backend/DB بهذه البيئة فتعذّر تسجيل دخول حي. |
| 2026-07-06 | T8.2 | SupplierRfqInbox.tsx (`/rfq/supplier-inbox`) كان ملفاً واحداً وظيفياً حقيقياً (710 سطر، تبويبا مباريات حصرية/سوق عام، عدّاد تنازلي لمهلة الرد، قبول/رفض) لكن غير مقسّم Desktop/Mobile وبثيم قديم primary-*/gray-* بدل supplier-*، وبلا أي عدّاد "منذ الوصول" (كان تاريخاً ثابتاً فقط). استخرجت useSupplierRfqInboxData.ts (كل منطق الجلب/المطابقة الدفعية/mutation القبول-الرفض + نبضة now كل 60 ثانية) و SupplierInboxCards.tsx (MatchCard/PublicRfqCard/CountdownTimer معاد تلوينها + ElapsedTimeBadge جديد يحسب الوقت منذ created_at بتلوين حسب الضغط الزمني: slate/كهرماني/أحمر)، ثم SupplierRfqInboxDesktop.tsx (شبكة بطاقات + EmptyState/Skeleton المشتركين) و SupplierRfqInboxMobile.tsx (عمود واحد مكدّس بتبويبات مضغوطة) + مبدّل SupplierRfqInbox.tsx. أبقيت زر "قدّم عرضاً" يفتح QuoteBuilderPage (BUILD_QUOTE) بدل تحويله للحاسبة العامة رغم أنها تدعم rfq_id كـquery param فعلياً — القرار وتفصيله تحت T8.2 أعلاه (عدم فقدان منطق بانٍ عروض حقيقي، نفس مبدأ T5.1/T8.1). أبقيت تبويبَي مباريات حصرية/سوق عام كوظيفة حقيقية موجودة أصلاً رغم أن نص الخطة يصف "قائمة" واحدة مبسّطة. 164/164 اختبار ناجح (`--no-file-parallelism`، 7 اختبارات جديدة)، tsc نظيف، curl عبر Vite يعيد 200 لكل الملفات الجديدة، لا primary-*/gray-*/indigo متبقية. **نهاية المرحلة 8 جزئياً — T8.3 إلى T8.7 متبقية.** |
| 2026-07-06 | T8.3 | QuotationListPage.tsx (`/quotes`، بلا RoleGuard) كان جدولاً واحداً وظيفياً حقيقياً غير مقسّم Desktop/Mobile، بثيم قديم primary-*/gray-*، وبشارات حالة يدوية بدل StatusPill المشترك كما يطلب النص. اكتشفت فجوة حقيقية: حالات العرض الفعلية (draft/pending/finalized/sent/accepted/rejected) لا تملك مقابل "مرفوض" في OrderStatus — أضفت قيمة rejected جديدة لـStatusPill.tsx (إضافة توافقية للخلف، كل الاختبارات القديمة استمرت + اختبار جديد). بنيت useQuotationListData.ts (خطاف مشترك: الجلب + quoteStatusPill/quoteTrackingStatus/TRACKING_LABELS) + QuotationListPageDesktop.tsx (جدول supplier-*/slate-* بـStatusPill/EmptyState/Skeleton) + QuotationListPageMobile.tsx (بطاقات مكدّسة بدل جدول عريض) + بوابة دور بـQuotationListPage.tsx: agent → الجديد، بقية الأدوار → LegacyQuotationList القديم كما هو (لحين T8.4 التي تبني واجهة المستورد على نفس الراوت). أبقيت (q as any).tracking_status كما هو (نفس نمط QuotationDetailPage/OrdersListPage، تصحيح النوع خارج نطاق المهمة). 173/173 اختبار ناجح (`--no-file-parallelism`، 9 اختبارات جديدة)، tsc نظيف، curl عبر Vite يعيد 200، لا primary-*/gray-*/indigo أو text-left/right ثابتة بالملفات الجديدة (الجدول القديم يحتفظ بـtext-right عمداً — كود غير مُلمَّس). |

