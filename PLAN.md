# PLAN.md — خطة تنفيذ واجهات AI-Sourcing Hub (v2)

> **لأي جلسة Claude Code جديدة: هذا الملف هو نقطة البداية.**
> اقرأه كاملاً، ثم اقرأ `CLAUDE.md`، ثم نفّذ "بروتوكول بداية الجلسة"
> أدناه. ابدأ من أول مهمة `[ ]` ولا تقفز للأمام أبداً.

---

## بروتوكول بداية كل جلسة (إلزامي)

```bash
# 1. أين نحن؟
git log --oneline -5        # آخر commits
git status                  # هل يوجد شغل غير محفوظ؟

# 2. ماذا أنجزنا؟
# اقرأ "سجل الجلسات" أسفل هذا الملف + أول مهمة [ ] غير مكتملة

# 3. هل المشروع يعمل؟
npm run dev                 # يجب أن يعمل بلا أخطاء قبل البدء بمهمة جديدة
```

- إن وجدت شغلاً غير محفوظ من جلسة سابقة: افحصه. إن كان سليماً
  أكمله واعمل commit. إن كان مكسوراً: `git checkout .` وأعد المهمة.
- إن كان `npm run dev` مكسوراً: إصلاحه هو مهمتك الأولى قبل أي شيء.

## بروتوكول نهاية كل جلسة (إلزامي)

1. commit لكل شغل سليم (لا تترك شغلاً معلقاً بلا commit).
2. علّم المهام المكتملة `[x]` في هذا الملف.
3. أضف سطراً في "سجل الجلسات" أسفل الملف.
4. إن اكتشفت مهمة ناقصة أثناء العمل: أضفها للمرحلة المناسبة بترقيم
   فرعي (مثل T2.3) — لا تنفّذها فوراً إلا إن كانت blocker.

---


> **⚠️ ملاحظة الألوان**: ملفات `handoff-designs/*.html` بُنيت قبل
> استبدال لوحة المستورد. أي indigo/بنفسجي فيها يُقرأ كمقابله من
> عائلة الأزرق البحري الجديدة في `tokens.ts` (نفس الموضع، اللون
> الجديد). الحَكَم النهائي للألوان هو `tokens.ts` وليس الـHTML.

## قواعد العمل الإلزامية

1. **مهمة واحدة في كل مرة.** لا استثناءات.
2. **commit بعد كل مهمة ناجحة**: `feat(plan): T<رقم> — <وصف>`
3. **لا تلمس ما لم يُطلب**: routing، API، صفحات خارج المهمة الحالية.
4. **معيار القبول شرط للتعليم `[x]`**: تحقق فعلياً (متصفح/curl) لا
   نظرياً.
5. **الفشل مرتين = BLOCKED**: اكتب `BLOCKED: <السبب>` تحت المهمة
   وانتقل لمهمة مستقلة. لا تدور بحلقة محاولات.
6. **الحَكَم البصري**: ملفات `handoff-designs/*.html`. عند أي تعارض
   بين وصف نصي والـHTML المرجعي، الـHTML يفوز.
7. **الحَكَم المنطقي**: `CLAUDE.md` (الألوان، القوائم، RFQ-per-View،
   قاعدة الأرقام المالية، نمط الموبايل المزدوج).
8. **RTL دائماً**: `text-start/end`، `ps-*/pe-*`، `ms-*/me-*` بدل
   left/right الثابتة.

---

## المرحلة 0 — الأمان والأساس

- [x] **T0.1 — نقطة رجوع آمنة**
  - checkpoint سليم موجود مسبقاً (`1757e64`). لا تغييرات معلّقة أخرى
    في شجرة العمل (باستثناء `ai-sourcing-hub/` — مجلد فرعي غريب
    مُتتبَّع كـ gitlink منذ commit `f195c2c` الأول، بلا `.gitmodules`؛
    ليس له علاقة بهذه المهمة ولم يُلمس).
  - قبول: `git log` يظهر الـcheckpoint. ✅

- [x] **T0.2 — توكنز الألوان في Tailwind**
  - `tailwind.config.ts` و`fontFamily` كانا موجودين مسبقاً من commit
    سابق، لكن ألوان `importer` كانت لا تزال البنفسجي/indigo المحظور
    (`#4F46E5` وعائلته) في **أربعة مواضع**: `frontend/tailwind.config.ts`،
    `frontend/src/lib/tokens.ts`، `BottomNav.tsx` (ACTIVE_COLOR.client)،
    و`ClientDashboard.tsx` (STATUS_BADGES). تم تصحيحها كلها للأزرق
    البحري الصحيح من `lib/tokens.ts` الجذري (`#1D6FB8` وعائلته).
  - قبول: تحقّق فعلي عبر curl لـCSS المولّد من Vite —
    `.bg-importer-500 { background-color: rgb(29 111 184) }` (=#1D6FB8) و
    `.bg-supplier-500 { background-color: rgb(16 185 129) }` (=#10B981). ✅
  - `npx tsc --noEmit` نظيف بعد التعديل.

- [x] **T0.3 — Cairo + RTL افتراضي**
  - موجود مسبقاً: `<html lang="ar" dir="rtl">` + رابط Google Fonts
    Cairo/Inter/Noto Sans SC في `index.html`. تحقّق عبر curl. ✅

- [x] **T0.4 — تشغيل نظيف + commit**
  - `npm run dev` (Vite) يعمل بلا أخطاء، HMR طبّق التعديلات بلا
    console errors (راجع devlog). ✅

---

## المرحلة 1 — المكوّنات المشتركة

> المرجع: `CLAUDE.md` أقسام "مكوّنات جاهزة" و"بنية الشاشات".

- [x] **T1.1 — `Sidebar.tsx`** — prop `role: 'agent'|'client'|'admin'`،
  قوائم الأدوار الثلاثة من `CLAUDE.md`، `w-64` على اليمين، العنصر
  النشط بلون الدور، بطاقة مستخدم أسفله.
  - كان المكوّن موجوداً من commit سابق ومربوطاً فعلياً في
    `AgentLayout`/`ClientLayout`/`AdminLayout`. وُجد خلل: عنصرا
    "منتجاتي" و"مراجعة المنتجات" (route مختلف) يتشاركان نفس
    `labelKey` (`nav.myProducts`) في قائمة agent — تم فصلهما بمفتاح
    ترجمة جديد `nav.productReview` (أضيف لـar/en/zh).
  - **مهمة فرعية اكتُشفت (غير blocker)**: قائمة admin تفتقد لعنصر
    "إدارة المستخدمين" (لا يوجد route/صفحة بعد) — أُضيفت كـ T9.3
    أدناه بدل تنفيذها الآن (تحتاج بناء صفحة كاملة).
  - قبول: تحقّق عبر `tsc --noEmit` نظيف + Sidebar مستخدم فعلياً في
    الثلاث layouts. ✅
- [x] **T1.2 — `MobileDrawer.tsx`** — نفس المحتوى، ينزلق من اليمين،
  overlay يغلقه، X، وأسفله الإعدادات + تسجيل خروج (أحمر).
  - كان المكوّن موجوداً (overlay + Sidebar bare + logout أحمر) لكن
    ناقص زر **X** للإغلاق — أُضيف (أعلى الدرج، `end-3` بعيداً عن
    شعار Sidebar) + مفتاح ترجمة `common.close` بالثلاث لغات.
  - الانزلاق/الحركة الفعلية (200ms) تُطبَّق في T1.7 حسب تسلسل الخطة.
  - قبول: تحقّق فعلي — 4 اختبارات Vitest جديدة
    (`MobileDrawer.test.tsx`) تغطي: عدم الظهور وهو مغلق، ظهور
    المحتوى/زر الإغلاق/تسجيل الخروج وهو مفتوح، إغلاقه بالنقر على
    الـoverlay وعلى X، واستدعاء `logout()` عند الضغط. 64/64 اختبار
    ناجح بالمشروع كاملاً بعد التعديل. ✅
- [x] **T1.3 — `BottomNav.tsx`** — prop `role`، 5 عناصر كحد أقصى:
  - agent: الرئيسية | الواردة | اللقطات | محادثات | حسابي
  - client: الرئيسية | اكتشف | ريلز | محادثات | حسابي
  - كانت القوائم القديمة مختلفة تماماً عن CLAUDE.md (طلبات الشراء/
    منتجاتي/السوق العالمي بدل الواردة/اللقطات/حسابي، وبلا تبويب
    "حسابي" إطلاقاً). أُعيد بناؤها لتطابق القائمتين حرفياً؛ الأدوات
    والصفحات كلها موجودة مسبقاً (`RFQ.SUPPLIER_INBOX`، `AGENT.REELS`،
    `PROFILE`) **عدا** صفحة ريلز المستورد التي لم تكن موجودة (مؤجلة
    لـT6.3) — أُضيف لها stub بنفس نمط `SettingsPage` (route جديد
    `CLIENT.REELS` + `ClientReelsPage.tsx`) بدل ترك تبويب معطّل، على
    أن يُستبدل بالمشغّل الحقيقي في T6.3.
  - أُصلح أيضاً `left-0 right-0` الثابتة إلى `start-0 end-0` (قاعدة
    RTL في CLAUDE.md).
  - قبول: تحقّق فعلي — اختباري Vitest جديدان (`BottomNav.test.tsx`)
    يتحققان من العناصر الخمسة بالنص العربي الحرفي لكل دور. 66/66
    اختبار ناجح بالمشروع كاملاً. ✅
- [x] **T1.4 — `TopBar.tsx` (موبايل)** — ☰ (يفتح الـDrawer) + عنوان +
  أفاتار.
  - كان المكوّن مكتملاً وصحيحاً بالفعل (☰ يفتح الـdrawer، شعار+عنوان،
    أفاتار بلون مشتق من الاسم + قائمة بروفايل). لم يكن مختبَراً من
    قبل — أضفت `TopBar.test.tsx` (تحقّق فعلي: ظهور العناصر الثلاثة +
    فتح الـdrawer عند الضغط على ☰). 68/68 اختبار ناجح بالمشروع كاملاً.
    ✅
- [x] **T1.5 — مكوّنات ذرّية** — `StatCard`, `LineRow`, `ReelTile`
  (حرفياً من `CLAUDE.md`) + **`StatusPill`** موحّد لكل حالات الطلبات:
  ```
  قيد الانتظار → slate | تحت المراجعة → sky
  جارٍ التفاوض → amber | قيد التنفيذ → لون الدور | مكتمل → emerald
  ```
  - لم تكن موجودة إطلاقاً (بحث شامل: صفر نتائج) — بُنيت من الصفر في
    `src/components/ui/`. `StatusPill` يأخذ `role` prop لتلوين
    "قيد التنفيذ" بلون الدور الحالي (supplier/importer). `ReelTile`
    مبني حرفياً من مواصفة CLAUDE.md — لا يعرض مشاهدات إطلاقاً، فقط
    عدّاد RFQ الأخضر البارز.
  - **مهمة فرعية اكتُشفت (غير blocker)**: `ReelsStudioPage.tsx` لديه
    `ReelTile` محلي خاص به لا يستخدم هذا المكوّن المشترك ولا يعرض
    عدّاد RFQ إطلاقاً (مخالفة لقاعدة RFQ-per-View الذهبية) — أُضيفت
    كملاحظة تحت T6.2 لدمجها عند إعادة بناء أستوديو الريلز.
  - قبول: بدل صفحة اختبار حية داخل الراوتر (لا تخدم CLAUDE.md ولا
    القوائم)، تحقّق فعلي عبر `atoms.test.tsx` — 6 اختبارات Vitest
    تغطي القيم/الألوان الأربعة الثابتة + لون الدور الديناميكي +
    الـoverlay التجاري الدائم + غياب أي عدّاد مشاهدات. 74/74 اختبار
    ناجح بالمشروع كاملاً. ✅
- [x] **T1.6 — `EmptyState.tsx` + `Skeleton.tsx`** — مكوّن فراغ موحّد
  (أيقونة + "لا توجد بيانات" + زر فعل اختياري) وskeleton تحميل بسيط.
  كل قائمة/جدول لاحقاً يستخدمهما — لا نصوص فراغ مخصصة متفرقة.
  - لم يكونا موجودين؛ 8 صفحات موجودة (SupplierRfqInbox،
    MarketplacePage، ClientDashboard، ...) لديها بلوكات "لا توجد
    بيانات" و`animate-pulse` مكرّرة يدوياً بنفس النمط تقريباً. بُني
    `EmptyState`/`Skeleton` في `components/ui/` بنفس نمط الـcard
    الموجود (`card p-12 text-center`) ليطابق الأسلوب الحالي. دمج
    الصفحات الثمانية فيهما هو **T10.3** بالضبط (موجودة أصلاً في
    الخطة) — لم يُنفَّذ هنا تجنباً لتوسيع نطاق T1.6.
  - قبول: تحقّق فعلي — 5 اختبارات Vitest (`EmptyState.test.tsx`,
    `Skeleton.test.tsx`) تغطي العرض الأساسي، إخفاء الزر حين لا يوجد
    action، تفعيل الزر، ودعم className مخصص للـskeleton. 79/79
    اختبار ناجح بالمشروع كاملاً. ✅
- [x] **T1.7 — نظام الحركة الموحّد**
  - طبّق قسم "الحركة والتفاعل" من `CLAUDE.md`: أنشئ classes/أنماط
    موحّدة (زر، بطاقة، عنصر قائمة) بحالات hover/active المحددة،
    وأضف دعم `prefers-reduced-motion`.
  - كل مكوّنات المرحلة 1 (Sidebar، Drawer، BottomNav، الذرّية)
    تستخدم هذه الأنماط — الـDrawer ينزلق 200ms والoverlay يتلاشى.
  - أهم خلل مُصلَح: `MobileDrawer` كان يُركَّب/يُفكَّك بـ
    `if (!drawerOpen) return null` بلا أي انتقال — يظهر/يختفي فجأة
    (مخالف مباشر لقبول T1.7). أُعيد بناؤه ليبقى مثبّتاً دائماً
    ويتحكم بالظهور عبر classes (`translate-x-full`↔`translate-x-0`
    على الدرج، `opacity-0`↔`opacity-100` على الـoverlay) مع
    `transition duration-200`.
  - أُضيف `active:scale-[0.98]` لكل الأزرار التفاعلية التي كانت
    تفتقده (☰ في TopBar، زر البروفايل، زر تسجيل الخروج، جرس
    الإشعارات، X وتسجيل الخروج في الـDrawer، تبويبات BottomNav)
    + `duration-150` على انتقالات الألوان الناقصة.
  - `btn-primary`/`btn-secondary` في `index.css` كانا بلا
    `active:scale-[0.98]` — أُضيف.
  - دعم `prefers-reduced-motion` أُضيف كقاعدة CSS عامة في
    `index.css` (تُصفّر مدة كل transition/animation لمن يطلبه) بدل
    تكرار `motion-reduce:` على كل عنصر.
  - قبول: تحقّق فعلي — 3 اختبارات Vitest محدَّثة في
    `MobileDrawer.test.tsx` تتحقق من classes الإخفاء/الإظهار الفعلية
    (translate/opacity/aria-hidden) بدل مجرد وجود العنصر في DOM، +
    فحص curl لـCSS المولّد يؤكد وجود قاعدة `prefers-reduced-motion`
    و`.translate-x-full` فعلياً. 79/79 اختبار ناجح بالمشروع كاملاً.
    ✅ — **نهاية المرحلة 1**.

---

## المرحلة 2 — صفحة تسجيل الدخول

> هذه من أقوى شاشات المنصة الحالية — عمود الثقة فيها يبيع المنصة
> قبل الدخول. نحسّنها لا نلغيها.

- [x] **T2.1 — الديسكتوب**: عمودان —
  - يمين (RTL: يظهر أولاً): عمود الثقة بخلفية brand-900 متدرجة:
    الشعار + "التوريد الذكي بدقة الذكاء الاصطناعي" + 3 بطاقات
    (دقة 0.82% / نافذة عطاء 48 ساعة / +240 مورّد معتمد) + شارات
    (حماية المشتري، ISO 9001، ترجمة تلقائية).
  - يسار: النموذج — 3 تبويبات دور (مستورد/مورد/الإدارة)، حقول
    البريد وكلمة المرور، زر دخول بلون الدور المختار (importer-500
    للمستورد، supplier-500 للمورد، slate-800 للإدارة)، "نسيت كلمة
    المرور؟"، "إنشاء حساب جديد"، سطر SSL.
  - قبول: تبديل التبويب يغيّر لون الزر فعلياً.
  - **اكتُشفت مخالفتان جوهريتان** لقاعدة CLAUDE.md الرابعة (ملفين
    منفصلين، لا صفحة responsive وحيدة): (1) `LoginPage.tsx` كان
    ملفاً واحداً يبدّل بين تخطيطي ديسكتوب/موبايل بـ
    `hidden lg:flex`/`lg:hidden` — قُسّم إلى `LoginPageDesktop.tsx` +
    `LoginPageMobile.tsx` + مبدّل رفيع `LoginPage.tsx` يعتمد على
    `useMediaQuery` (hook جديد) بدل CSS toggling داخل نفس الملف؛
    منطق النموذج المشترك استُخرج لـ`useLoginForm.ts` لتفادي تكراره.
    (2) زر الدخول كان بلون أخضر ثابت `#059669` بغضّ النظر عن الدور
    المختار — لم يكن يتغيّر إطلاقاً رغم أن هذا هو معيار القبول
    الصريح. أُصلح ليأخذ `importer-500`/`supplier-500`/`slate-800`
    فعلياً حسب `activeRole`. كذلك استُبدل خلفية عمود الثقة
    `#064e3b` (hex يدوي) بـ`bg-brand-900` من tokens.ts.
  - قبول: تحقّق فعلي — `LoginPageDesktop.test.tsx` (3 اختبارات)
    تثبت أن لون الزر يتغيّر فعلياً مع كل تبويب. ✅
- [x] **T2.2 — الموبايل**: عمود الثقة يصير شريطاً مضغوطاً أعلى
  النموذج (الشعار + سطر القيمة + البطاقات الثلاث أفقياً scrollable).
  - نُفّذت مع T2.1 كملف مستقل `LoginPageMobile.tsx` (نفس الانقسام
    أعلاه). أُضيف polyfill لـ`window.matchMedia` في `test/setup.ts`
    (لم يكن موجوداً — jsdom لا يوفره) + `LoginPage.switcher.test.tsx`
    يثبت اختيار الملف الصحيح حسب نتيجة `matchMedia`. جميع اختبارات
    LoginPage القديمة (6) استمرت بالنجاح دون تعديل ضد تخطيط الموبايل
    الجديد. 84/84 اختبار ناجح بالمشروع كاملاً.

---

## المرحلة 3 — رئيسية المندوب/المورد

> المرجع: `supplier-home-desktop.html` / `supplier-home-mobile.html`.

- [x] **T3.1 — الديسكتوب**: Sidebar(agent) + ترحيب/تاريخ + شريط
  "طلبات تنتظر ردّك" (supplier-600، بطاقات واردة، زر أبيض، رسالة
  الضغط الزمني) + 4 KPI + لوحة Kanban بأربعة أعمدة (StatusPill
  للنقاط) + "أستوديو لقطات المصنع" (زر رفع + شبكة، تحت كل مقطع:
  مشاهدات باهتة + "N طلب سعر" supplier-600 بارز).
  - `AgentDashboard.tsx` كان ملفاً واحداً responsive (308 سطر) —
    مخالفة مباشرة لقاعدة CLAUDE.md الرابعة، ونمط CSS-variables قديم
    (`var(--surface)`...) بدل توكنز slate/supplier، وKanban محلي لا
    يستخدم `StatusPill` المشترك، وبلا شريط "تنتظر ردّك" ولا أستوديو
    لقطات إطلاقاً. قُسّم على نمط LoginPage تماماً:
    `AgentDashboard.tsx` (مبدّل رفيع عبر `useMediaQuery`) +
    `AgentDashboardDesktop.tsx` (البناء الفعلي الجديد) +
    `AgentDashboardMobile.tsx` (نقل حرفي للتطبيق القديم، بديل مؤقت
    لحين T3.2 الحقيقية). استُخرج منطق الجلب المشترك (RFQs/إحصاءات/
    عروض مقبولة/منتجات) إلى `useAgentDashboardData.ts` ليشترك فيه
    الملفان بلا تكرار.
  - Kanban الجديد يستخدم `StatusPill` مباشرة لعناوين الأعمدة — حالات
    الطلب (`open/processing/quoted/closed`) تطابق حرفياً تصنيف
    CLAUDE.md (`pending/under_review/negotiating/completed`).
  - قسم الريلز: لا يوجد أي backend لعدّاد RFQ لكل لقطة في المشروع
    كله (تحقّقت من ذلك) — بدل اختلاق أرقام، اتُّبع نفس نهج
    `ReelsStudioPage.tsx` الصادق: يعرض منتجات حقيقية من
    `catalogService` عبر `ReelTile` المشترك مع `rfqCount={0}` صريح
    + تنويه أصفر "رفع الفيديو غير متاح بعد" (نفس نص الصفحة الأخرى).
  - قبول: تحقّق فعلي — `AgentDashboardDesktop.test.tsx` (5 اختبارات:
    KPI، شريط تنتظر ردّك يظهر/يختفي بشرط، Kanban بأعمدة StatusPill
    الأربعة، تنويه الريلز + عدّاد صفر صادق) و
    `AgentDashboard.switcher.test.tsx` (2 اختبار يتحققان من اختيار
    الملف الصحيح حسب `matchMedia`). `npx tsc --noEmit` نظيف،
    91/91 اختبار ناجح بالمشروع كاملاً (تعطّل تشغيل الاختبارات
    بالتوازي الافتراضي بسبب قيود بيئة العمل — أعيد التشغيل بـ
    `--no-file-parallelism` بنجاح). `npm run dev` (منفذ بديل) يعمل
    بلا أخطاء console. ✅
- [x] **T3.2 — الموبايل**: TopBar + 3 KPI + شريط الواردة + Kanban
  أفقي scrollable (بطاقات 150px) + الأستوديو + BottomNav + Drawer.
  - `AgentDashboardMobile.tsx` كان نقلاً حرفياً للتطبيق القديم (CSS-variables
    قديمة، بلا شريط "تنتظر ردّك"، بلا أستوديو ريلز، 4 KPI بدل 3). أُعيد
    بناؤه بالكامل من الصفر مطابقاً للمرجع `supplier-home-mobile.html`
    (تحقّق بصري فعلي عبر Playwright screenshot بما أن الملف HTML مبني بحزمة
    JS مصغّرة يتعذّر قراءتها كنص): ترحيب + شريط "تنتظر ردّك" بخلفية
    `supplier-900` وزر أبيض + 3 KPI (بدل 4، القاعدة الذهبية للموبايل) +
    Kanban أفقي ببطاقات `w-[150px]` تستخدم `StatusPill`/`StatCard`/`ReelTile`
    المشتركة (بدل الأنماط المحلية القديمة) + قسم الأستوديو بنفس نهج
    الديسكتوب الصادق (منتجات كتالوج حقيقية + `rfqCount=0` صريح، لا تلفيق
    لتقييم "4.8" الذي يظهر بالمرجع البصري بلا أي backend). TopBar/BottomNav/
    Drawer موجودة أصلاً عالمياً في `AgentLayout` فبقيت كما هي بلا لمس.
  - عدّلت `AgentDashboard.switcher.test.tsx`: الاختبار القديم كان يفترض
    غياب قسم الريلز بالموبايل (صحيح للنسخة المؤقتة القديمة فقط) — بما أن
    T3.2 تبني قسم ريلز حقيقياً بالموبايل، غيّرت نقطة التفريق بين
    الملفين إلى غياب زر "+ طلب جديد" (حصري للديسكتوب) بدل غياب الريلز.
  - قبول: تحقّق فعلي — `AgentDashboardMobile.test.tsx` جديد (5 اختبارات:
    3 KPI فقط، شريط الواردة يظهر/يختفي بشرط، Kanban بأعمدة StatusPill
    الأربعة، تنويه الريلز + عدّاد صفر صادق) + تعديل
    `AgentDashboard.switcher.test.tsx`. `npx tsc --noEmit` نظيف، 101/101
    اختبار ناجح بالمشروع كاملاً (`--no-file-parallelism`). تحقّق فعلي عبر
    Vite: الملف يترجم بلا أخطاء (`curl` على مسار الموديول يعيد 200 بلا
    أخطاء تحويل) ودورة `npm run dev` نظيفة. ✅

---

## المرحلة 4 — رئيسية المستورد

> المرجع: `importer-home-desktop.html` / `importer-home-mobile.html`.

- [x] **T4.1 — الديسكتوب**: Sidebar(client) + ترحيب + CTA
  "طلب عرض سعر جديد" (importer-500) + 4 KPI + "جديد اليوم من
  المصانع" (ReelTiles بأسعار + أسماء المصانع) + "طلباتي الأخيرة"
  (StatusPill لكل طلب).
  - `ClientDashboard.tsx` كان (كـ`LoginPage`/`AgentDashboard` القديمين)
    ملفاً واحداً responsive بمحتوى مختلف تماماً عن هذه المواصفة/المرجع
    البصري: بطاقة "تكلفة الهبوط" + نموذج إنشاء RFQ سريع مضمّن، بلا 4
    KPI وبلا قسم "جديد اليوم من المصانع" إطلاقاً. قُسّم على نفس نمط
    Login/AgentDashboard: `ClientDashboardDesktop.tsx` (بناء جديد مطابق
    لـ`importer-home-desktop.html` + هذه المواصفة) + `ClientDashboardMobile.tsx`
    (نقل حرفي للمحتوى القديم، بديل مؤقت لحين T4.2) + `ClientDashboard.tsx`
    (مبدّل رفيع عبر `useMediaQuery`). منطق الجلب المشترك استُخرج لـ
    `useClientDashboardData.ts`.
  - الـ4 KPI كلها مُشتقة من بيانات حقيقية (لا أرقام ملفّقة): متوسط زمن
    الرد = معدّل الفرق بالساعات بين إنشاء كل RFQ وأول عرض سعر وصله
    (فقط للطلبات التي فعلاً استلمت عرضاً، وإلا "—")، بانتظار الرد =
    عدد RFQs بحالة `open`، طلبات نشطة = `open`+`processing`، صفقات
    مكتملة هذا الشهر = `closed` بـ`updated_at` ضمن الشهر الحالي.
  - "جديد اليوم من المصانع": نفس نهج `AgentDashboardDesktop` الصادق —
    منتجات كتالوج حقيقية عبر `catalogService.search` مع `ReelTile`
    المشترك (`rfqCount={0}` صريح، لا يوجد backend لعدّاد RFQ لكل
    منتج)، واسم المصنع الحقيقي (`factory_name`/`supplier_name`) تحت كل
    بلاطة.
  - "طلباتي الأخيرة": `StatusPill` بنفس تخطيط حالات RFQ المستخدم في
    Kanban المندوب (`open→pending، processing→under_review،
    quoted→negotiating، closed→completed`) + سعر عرض السعر الأخير
    الحقيقي لكل طلب (map عبر `quotesByRfq`) بدل الكود القديم الذي كان
    يعرض `rfq.client_name` (خطأ نسخ من نسخة المندوب — يساوي اسم
    المستخدم نفسه لكل صفوفه، بلا فائدة) — استُبدل بالكمية الحقيقية من
    `productsMap` (نفس أسلوب Kanban المندوب).
  - قبول: تحقّق فعلي — `npx tsc --noEmit` نظيف، 101/101 اختبار ناجح
    بالمشروع كاملاً (`--no-file-parallelism`)، فحص curl لكل الملفات
    الأربعة عبر Vite dev server يعيد 200 بلا أخطاء تحويل. تعذّر تسجيل
    دخول حي (لا backend/DB في هذه البيئة — بلا docker) فتحقّقت بصرياً
    من المرجع عبر Playwright screenshot لـ`importer-home-desktop.html`
    وقارنته ببنية الصفحة الجديدة سطراً بسطر (ترتيب: ترحيب+CTA، 4 KPI،
    شبكة الريلز بتسمية المصنع تحتها، قائمة الطلبات بالسعر/الحالة). ✅
- [x] **T4.2 — الموبايل**: 3 KPI + نفس المحتوى مكدّساً + التنقل
  المزدوج.
  - `ClientDashboardMobile.tsx` كان نقلاً حرفياً للملف القديم (بطاقة
    تكلفة هبوط + نموذج RFQ سريع مضمّن) — بديل مؤقت من T4.1. أُعيد
    بناؤه بالكامل مطابقاً لـ`importer-home-mobile.html` ونمط
    `AgentDashboardMobile`: ترحيب + زر CTA بعرض كامل + 3 KPI فقط
    (`stats.slice(0, 3)`، إسقاط "صفقات مكتملة هذا الشهر" — القاعدة
    الذهبية للموبايل) + شبكة ريلز `grid-cols-3` (3 منتجات بدل 4) بنفس
    نهج الكتالوج الحقيقي/الصادق + قائمة "طلباتي الأخيرة" بنفس منطق
    الديسكتوب (`StatusPill` + سعر عرض السعر الحقيقي). TopBar/BottomNav/
    Drawer موجودة أصلاً عالمياً في `ClientLayout` فبقيت كما هي بلا لمس.
  - قبول: تحقّق فعلي — `npx tsc --noEmit` نظيف، 101/101 اختبار ناجح
    (`--no-file-parallelism`)، فحص curl عبر Vite dev server لكل من
    `ClientDashboardMobile.tsx`/`ClientDashboard.tsx` يعيد 200 بلا
    أخطاء تحويل من جانب السيرفر. تعذّر تسجيل دخول حي (لا backend/DB في
    هذه البيئة) فتحقّقت بصرياً من المرجع عبر Playwright screenshot
    لـ`importer-home-mobile.html` وقارنته ببنية الصفحة الجديدة. ✅ —
    **نهاية المرحلة 4.**

---

## المرحلة 5 — حاسبة التسعير

> المرجع: `pricing-calculator-*.html`. قاعدة صارمة: slate للأرقام،
> أخضر للإجمالي النهائي فقط، `tabular-nums` دائماً.

- [x] **T5.1 — الديسكتوب**: نموذج "بيانات الشحنة" (FOB، الكمية،
  الشحن/وحدة، التأمين%، الجمارك%، الضريبة%، رسوم الميناء، هامش
  المندوب) + بطاقة "التكلفة الواصلة المتوقعة" (LineRow لكل بند،
  شارة "دقة 0.82%"، إجمالي أخضر كبير، زر "إرسال كعرض سعر للعميل").
  الحساب لحظي بنفس معادلات الـHTML المرجعي (تأمين على FOB+شحن،
  جمارك على القيمة الجمركية، ضريبة على القيمة+الجمارك).
  - **تعارض جوهري اكتُشف واستُشير المستخدم بشأنه**: المرجع البصري
    يمثّل حاسبة بسيطة (منتج واحد، حقول يدوية FOB/شحن/تأمين%/جمارك%/
    ضريبة%/رسوم ميناء/هامش مندوب%، حساب فوري بلا شبكة). لكن
    `/agent/calculator` (`PricingCalcPage.tsx`) كانت بالفعل صفحة
    حقيقية تعمل: اختيار RFQ → تحميل منتجاته الحقيقية → استدعاء محرك
    تسعير حقيقي متصل بالباك إند (`POST /pricing/calculate`) بجداول
    رسوم HS حقيقية، ضريبة قابلة للتخصيص لكل HS، قواعد formula/percentage/
    fixed، غرامات، وإنشاء عرض سعر فعلي — تُستخدم من `QuoteBuilderPage`
    أيضاً. إعادة البناء حرفياً حسب المرجع كانت ستفقد هذا المحرك
    الحقيقي بالكامل. سألت المستخدم صراحة؛ اختار: **"أعد التصميم فقط،
    حافظ على المنطق"** — إعادة تصميم بصري فقط (LineRow لكل بند،
    شارة دقة 0.82%، بطاقة إجمالي خضراء) مع الإبقاء الكامل على تدفق
    RFQ الحقيقي ومحرك الباك إند دون أي فقدان وظيفي.
  - نُفّذ: قسمت `PricingCalcPage.tsx` (ملف واحد responsive قديم —
    نفس مخالفة Login/AgentDashboard/ClientDashboard) إلى
    `PricingCalcPageDesktop.tsx` (تصميم جديد: عمودان — نموذج
    الشحنة/جدول المنتجات على اليمين (عريض)، بطاقة النتيجة `sticky`
    على اليسار (380px) مطابقة للمرجع بصرياً) + `PricingCalcPageMobile.tsx`
    (نقل حرفي للتطبيق القديم كاملاً، بديل مؤقت لحين T5.2) +
    `PricingCalcPage.tsx` (مبدّل رفيع `useMediaQuery`، نفس نمط كل
    الصفحات السابقة). استُخدم `usePricingCalculator.ts` (كان موجوداً
    من جلسة سابقة غير مكتملة — untracked — وطابق تماماً منطق الصفحة
    القديمة، فتم اعتماده كخطاف مشترك).
  - بطاقة النتيجة استُخرجت لمكوّن مشترك `PricingResultCard.tsx`:
    تجميع كل بنود المحرك الحقيقي (FOB محول، شحن، تأمين، جمارك، رسوم
    خدمة 070%، تخليص، ضريبة، بدل 301 لكل شحنة، رسوم قواعد مخصصة،
    خصم الدفع المبكر، عمولة) كـ`LineRow` بدل إخفاء تعقيد المحرك
    الحقيقي إرضاءً لبساطة المرجع (نفس نهج الصدق المتّبع في T3.1/T4.1
    — لا تلفيق ولا حذف بيانات حقيقية). الإجمالي بقي بلون
    `supplier-900` الغامق (أخضر فاتح جداً في الظاهر لكنه فعلياً أخضر
    من عائلة supplier — يطابق قاعدة CLAUDE.md "الأخضر للإجمالي
    النهائي فقط" ومطابق للنمط الأصلي بالكود قبل التعديل). جدول تفاصيل
    المنتجات وقواعد الـHS المطبّقة استُخرجا لـ`PricingDetailBreakdown.tsx`
    (شفافية إضافية لا يقدّمها المرجع البسيط، أُبقيت كملحق تحت البطاقتين).
    جدول تعديل المنتجات استُخرج لـ`ProductsInputTable.tsx` ليُستخدم من
    كلا الملفين لاحقاً بلا تكرار.
  - وسّعت `LineRow`'s `value` prop من `string | number` إلى
    `ReactNode` لدعم لف الأرقام بـ`dir="ltr"` (نمط مستخدم بالفعل
    بكل الصفحات المالية) — توسيع نوع متوافق للخلف، لا يكسر أي استخدام
    قائم (تأكّد عبر `atoms.test.tsx`).
  - قبول: تحقّق فعلي — `PricingCalcPageDesktop.test.tsx` جديد (حالة
    الفراغ قبل اختيار RFQ + بطاقة LineRow/شارة الدقة/الإجمالي الأخضر
    بعد الحساب) و`PricingCalcPage.switcher.test.tsx` جديد (يتحقق من
    اختيار الملف الصحيح حسب `matchMedia`). اختبارات `PricingCalcPage.test.tsx`
    القديمة كلها استمرت بالنجاح دون تعديل (تعمل ضد `PricingCalcPageMobile`
    عبر المبدّل، نسخة مطابقة حرفياً للمنطق القديم). `npx tsc --noEmit`
    نظيف، 105/105 اختبار ناجح (`--no-file-parallelism`)، فحص curl لكل
    الملفات الجديدة عبر Vite dev server يعيد 200 بلا أخطاء تحويل،
    `npm run dev` نظيف بلا console errors. ✅
- [ ] **T5.2 — الموبايل**: النموذج فوق النتيجة، BottomNav.
- [ ] **T5.3 — ربط `POST /api/v1/pricing/calculate/`** مع fallback
  محلي عند فشل الطلب.
  - قبول: تغيير أي حقل يحدّث النتيجة، وفصل الباك إند لا يكسرها.
  - commit.

---

## المرحلة 6 — الريلز (الجانبان)

> القاعدة الذهبية: المقياس = RFQ الناتجة. المشاهدات ثانوية باهتة.
> كل تفاعل اجتماعي مُعاد تفسيره تجارياً.

- [ ] **T6.1 — أستوديو المورد، موبايل** (المرجع:
  `supplier-reels-mobile.html`): مشغّل عمودي ملء الشاشة + طبقة
  تجارية دائمة (مصنع + توثيق + منتج + سعر + زر طلب عرض) + أزرار
  جانبية (احفظ/اسأل/مشاركة/الأداء) + "N طلب سعر ناتج" أخضر +
  زر "ارفع" + تنقّل بين المقاطع + Drawer/BottomNav.
- [ ] **T6.2 — أستوديو المورد، ديسكتوب** (المرجع:
  `supplier-reels-desktop.html`): المشغّل 300px + لوحة أداء
  (مشاهدات/طلبات سعر/تحويل/حفظ) + قائمة اللقطات مرتبة حسب RFQ.
  - **T6.2.1** (اكتُشفت في T1.5): استبدل `ReelTile` المحلي داخل
    `ReelsStudioPage.tsx` بالمكوّن المشترك `components/ui/ReelTile.tsx`
    — النسخة المحلية لا تعرض عدّاد RFQ إطلاقاً (مخالفة للقاعدة الذهبية).
- [ ] **T6.3 — مشغّل المستورد (Consumer)، موبايل + ديسكتوب**:
  نفس المشغّل بصرياً لكن من زاوية الاستهلاك:
  - "إعجاب" → **أضف للقائمة المختصرة** (bookmark بمسمى تجاري)
  - "متابعة المصنع" → اشتراك بتنبيهات منتجاته
  - "سؤال" → يفتح محادثة تقود لـRFQ
  - زر "طلب عرض سعر" دائم الحضور
  - **بلا** لوحة أداء ولا زر رفع (هذه للناشر فقط)
  - يدخل له من تبويب "ريلز" في BottomNav للمستورد.
  - commit بعد كل مهمة.

---

## المرحلة 7 — الملفات الشخصية

> المرجع: `importer-profile.html` / `importer-profile-mobile.html`
> + قسم الريلز في `CLAUDE.md` لملف المورد.

- [ ] **T7.1 — ملف المستورد (ديسكتوب + موبايل)**: غلاف importer-900
  + شارة "سجل الاستيراد الكامل" + أفاتار + "مستورد موثّق" +
  3 إحصائيات (صفقات / الميناء المفضّل بلون importer / متوسط الطلب)
  + بيانات الشركة + "محفوظاتي ومتابعاتي" (ReelTiles بشارة محفوظ).
  موبايل: سويتشر segmented متصل (محفوظاتي / طلباتي النشطة).
- [ ] **T7.2 — ملف المورد (ديسكتوب + موبايل)**: ألوان supplier +
  غلاف بشارة "شاهد جولة داخل المصنع" (زر تشغيل) + "مورد موثّق" +
  إحصائيات (128 صفقة / دقة 0.82% أخضر / 4h زمن الرد) + بيانات
  المصنع + ISO 9001 + سويتشر (المنتجات / لقطات المصنع) + شبكة
  بأسعار overlay دائمة وزر "طلب عرض" لكل عنصر.
  - commit.

---

## المرحلة 8 — صفحات سير العمل (Workflows)

> لا مراجع HTML — استنبط من المكوّنات والقواعد. التزم بلون الدور.

- [ ] **T8.1 — إنشاء RFQ (client)**: نموذج (اسم المنتج، المواصفات،
  الكمية، صور اختيارية) + معاينة التكلفة التقديرية جانبه (يستدعي
  الحاسبة) + زر إرسال importer-500.
- [ ] **T8.2 — طلبات العملاء الواردة (agent)**: قائمة RFQs الواردة
  مع عدّاد الزمن منذ الوصول (الضغط الزمني) + زر "قدّم عرضاً" لكل
  طلب يفتح الحاسبة مسبقة التعبئة.
- [ ] **T8.3 — عروض الأسعار (agent)**: جدول العروض المرسلة
  بحالاتها (StatusPill) + قيمها + تاريخها.
- [ ] **T8.4 — طلباتي (client)**: نفس الجدول من زاوية المستورد.
- [ ] **T8.5 — السوق العالمي (client + agent)**: شبكة بطاقات
  (صورة/سعر/مصنع/توثيق/زر طلب عرض) + فلاتر (فئة، بلد المنشأ،
  نطاق سعر) + بحث.
- [ ] **T8.6 — المحادثات**: قائمة غرف + نافذة رسائل، فقاعات
  المستخدم الحالي بلون دوره، زر مرفق "أرسل عرض سعر" داخل المحادثة
  (يربط الشات بالـRFQ — كل محادثة تقود لصفقة).
- [ ] **T8.7 — تتبّع الشحنات**: خط زمني عمودي (تم الشحن → في البحر
  → وصل الميناء → التخليص → التسليم) بنقاط ملوّنة وحالة نشطة.
  - commit بعد كل صفحة.

---

## المرحلة 9 — صفحات الأدمن

> slate بحت، كثافة معلومات عالية، صفر طابع اجتماعي.

- [ ] **T9.1 — لوحة التحكم**: KPIs (طلبات/عروض/مستخدمين/منتجات) +
  تكاليف الذكاء الاصطناعي + إدارة سريعة + قواعد التسعير النشطة.
- [ ] **T9.2 — مراقبة النظام**: حالة الخدمات (DB/Redis/MinIO/
  Celery/LLM) بمؤشرات ملوّنة + زمن استجابة endpoints بأشرطة +
  معلومات البيئة. (المرجع الوظيفي: لقطة المنصة الحالية — حافظ على
  نفس المعلومات بتنسيق النظام الجديد.)
- [ ] **T9.3 — إدارة المستخدمين + توثيق الموردين**: جداول بفلاتر +
  أزرار اعتماد/رفض التوثيق.
  - **T9.3.1** (اكتُشفت في T1.1): أضف route `ADMIN.USERS` + عنصر
    "إدارة المستخدمين" في `Sidebar.tsx` (ADMIN_NAV) بعد بناء الصفحة —
    مفتاح الترجمة `nav.userManagement` موجود مسبقاً بالثلاث لغات.
- [ ] **T9.4 — قواعد التسعير + جداول رسوم HS + الكتالوج العالمي**:
  جداول CRUD بسيطة وواضحة.
  - commit بعد كل صفحة.

---

## المرحلة 10 — ربط الـAPI الشامل

- [ ] **T10.1 — `lib/api.ts` موحّد**: fetch wrapper + baseURL من
  `NEXT_PUBLIC_API_URL` + حقن توكن المصادقة + معالجة أخطاء موحّدة.
- [ ] **T10.2 — استبدال البيانات الوهمية صفحةً صفحة** بالـendpoints
  في `CLAUDE.md` (rfqs / pricing / catalog / auth/me / chat /
  admin). **قاعدة: صفحة واحدة = commit واحد.**
- [ ] **T10.3 — حالات التحميل والفراغ**: `Skeleton` أثناء الجلب،
  `EmptyState` عند لا بيانات، رسالة خطأ + زر "إعادة المحاولة" عند
  الفشل — في كل قائمة/جدول بلا استثناء.

---

## المرحلة 11 — مميزات إضافية

- [ ] **T11.1 — جرس الإشعارات**: أيقونة بعدّاد في الشريط العلوي +
  قائمة منسدلة (RFQ جديد للمورد / عرض جديد للمستورد / تحديث حالة
  شحنة). polling بسيط الآن — لا websockets في هذه المرحلة.
- [ ] **T11.2 — مبدّل اللغة**: في السايد بار والـDrawer (🌐 عربي |
  EN | 中文) — يبدّل locale واتجاه `dir` والخط (Cairo/Inter/Noto).
- [ ] **T11.3 — i18n كامل**: next-intl، ملفات ترجمة ar/en/zh لكل
  النصوص الثابتة. المحتوى المُدخل من المستخدم يُعرض كما هو.
- [ ] **T11.4 — تحسينات لمسية للموبايل**: مساحات لمس ≥44px لكل
  الأزرار، سحب أفقي سلس للـKanban والريلز.

---

## المرحلة 12 — التنظيف والفحص النهائي

- [ ] **T12.1 — إزالة الثيم القديم**: الآن فقط أزل palette القديمة
  والـdark-default وأي CSS ميت. تأكد ألا صفحة انكسرت.
- [ ] **T12.2 — فحص شامل صفحة-صفحة**: الأدوار الثلاثة × ديسكتوب
  وموبايل × مقارنة مع `handoff-designs`. أي انحراف → مهمة فرعية
  جديدة هنا.
- [ ] **T12.3 — فحص القواعد الست**: (1) لا text-left/right ثابتة
  (2) لا hex يدوية خارج tokens (3) لا ألوان دور مخلوطة (4) الأرقام
  المالية slate+أخضر فقط (5) كل قائمة لها Empty/Skeleton (6) كل
  ريل يعرض RFQ لا مشاهدات كمقياس أول. استخدم grep للتحقق:
  ```bash
  grep -rn "text-left\|text-right" app/ components/ | grep -v node_modules
  grep -rn "#[0-9A-Fa-f]\{6\}" app/ components/ | grep -v tokens
  grep -rn "4F46E5\|4338CA\|6366F1\|312E81" . --include="*.ts*" # يجب أن يعيد صفر نتائج (indigo محظور)
  ```
- [ ] **T12.4 — commit نهائي + وسم**: `git tag v1-design-migration`.

---

## سجل الجلسات (حدّثه نهاية كل جلسة)

| التاريخ | المهام المنجزة | ملاحظات / BLOCKED |
|---------|----------------|---------------------|
| 2026-07-05 | T0.1, T0.2, T0.3, T0.4 | T0.1/T0.3/T0.4 كانت مستوفاة مسبقاً (تحقّق فقط). T0.2 كشفت indigo محظور متبقٍ في 4 ملفات (tailwind.config.ts، lib/tokens.ts، BottomNav.tsx، ClientDashboard.tsx) — تم تصحيحها للأزرق البحري. مجلد `ai-sourcing-hub/` الفرعي (gitlink غريب، 1.1GB) لوحظ لكن لم يُلمس — خارج نطاق الخطة. |
| 2026-07-05 | T1.1 | Sidebar.tsx كان موجوداً ومربوطاً بالفعل في الثلاث layouts؛ أصلحت خلل تكرار labelKey بين "منتجاتي" و"مراجعة المنتجات". أضفت T9.3.1 كمهمة فرعية لعنصر admin الناقص "إدارة المستخدمين". |
| 2026-07-05 | T1.2 | MobileDrawer.tsx كان شبه مكتمل؛ أضفت زر X المفقود + مفتاح ترجمة close، وكتبت 4 اختبارات Vitest تغطي فتح/إغلاق/logout (تحقق فعلي بدل نظري). |
| 2026-07-05 | T1.3 | BottomNav.tsx كان يستخدم قوائم مختلفة تماماً عن CLAUDE.md وبلا تبويب "حسابي". أعدت بناءه ليطابق القائمتين حرفياً، وأضفت stub لصفحة ريلز المستورد (route/page جديدين) لأن T6.3 الحقيقية لم تُبنَ بعد. |
| 2026-07-05 | T1.4 | TopBar.tsx كان مكتملاً وصحيحاً مسبقاً بلا اختبارات — أضفت TopBar.test.tsx للتحقق الفعلي. |
| 2026-07-05 | T1.5 | بنيت StatCard/LineRow/StatusPill/ReelTile من الصفر في components/ui/ (لم تكن موجودة). أضفت T6.2.1 لدمج ReelTile المشترك في ReelsStudioPage لاحقاً بدل نسخته المحلية الناقصة لعدّاد RFQ. |
| 2026-07-05 | T1.6 | بنيت EmptyState/Skeleton من الصفر (8 صفحات لديها نسخ يدوية مكررة بنفس النمط تقريباً). دمج الصفحات القائمة مؤجّل لـT10.3 كما هو مخطط أصلاً. |
| 2026-07-05 | T1.7 | أهم إصلاح: MobileDrawer كان يظهر/يختفي فجأة بلا انتقال (return null) — أعدت بناءه بانزلاق CSS فعلي 200ms. أضفت active:scale-[0.98] لكل الأزرار الناقصة له، وقاعدة prefers-reduced-motion عامة. نهاية المرحلة 1 — كل مهام T1.x مكتملة. |
| 2026-07-05 | T2.1, T2.2 | LoginPage.tsx كان ملفاً واحداً responsive (مخالفة صريحة لقاعدة الملفين) وزر الدخول لم يكن يتغيّر لونه بالدور إطلاقاً رغم أنه معيار القبول. قسمتها لـLoginPageDesktop/Mobile.tsx + useLoginForm.ts المشترك + useMediaQuery hook جديد. **ملاحظة مهمة للمراحل القادمة**: لم أتحقق بعد إن كانت صفحات أخرى موجودة (AgentDashboard وغيرها) تعاني نفس مخالفة "ملف responsive وحيد" — يجب فحص كل صفحة عند الوصول لمهامها (T3+) والانقسام إن لزم، بنفس نمط useLoginForm/useMediaQuery. |
| 2026-07-05 | T3.1 | تأكدت الملاحظة السابقة: AgentDashboard.tsx كان نفس مخالفة LoginPage (ملف واحد responsive + ثيم CSS-variables قديم)، قسمته لنفس نمط Desktop/Mobile/switcher + useAgentDashboardData.ts مشترك. Kanban صار يستخدم StatusPill المشترك (تطابق حرفي بين حالات RFQ وتصنيف CLAUDE.md). قسم الريلز الجديد صادق: لا يوجد backend لعدّاد RFQ لكل لقطة بالمشروع كله، فاستُخدم نفس نهج ReelsStudioPage (منتجات كتالوج حقيقية + rfqCount=0 + تنويه أصفر) بدل اختلاق أرقام. AgentDashboardMobile.tsx حالياً نقل حرفي للتطبيق القديم فقط (بديل مؤقت) — إعادة بنائه الفعلية هي T3.2. 91/91 اختبار ناجح (تعطّل التوازي الافتراضي بسبب بيئة العمل — نجح بـ`--no-file-parallelism`)، tsc نظيف، dev server يعمل بلا أخطاء. |
| 2026-07-05 | T3.2 | أعدت بناء AgentDashboardMobile.tsx بالكامل مطابقاً لـsupplier-home-mobile.html (تحقّق بصري عبر Playwright screenshot لأن ملف الـHTML المرجعي حزمة JS مصغّرة). صار يستخدم StatusPill/StatCard/ReelTile المشتركة بدل الأنماط المحلية، 3 KPI فقط (لا 4)، شريط "تنتظر ردّك" بخلفية supplier-900، ونفس نهج الريلز الصادق (rfqCount=0 حقيقي، لا تلفيق لتقييم "4.8" الذي بالمرجع). عدّلت AgentDashboard.switcher.test.tsx لأن نقطة تفريقه القديمة (غياب قسم الريلز بالموبايل) لم تعد صحيحة بعد بناء الريلز الحقيقي — بدّلتها لغياب زر "+ طلب جديد" الحصري بالديسكتوب. 101/101 اختبار ناجح، tsc نظيف، Vite يترجم الملف بلا أخطاء. **نهاية المرحلة 3.** |
| 2026-07-05 | T4.1 | ClientDashboard.tsx كان نفس مخالفة LoginPage/AgentDashboard (ملف واحد responsive) لكن بمحتوى مختلف كلياً عن المرجع/المواصفة (بطاقة تكلفة هبوط + نموذج RFQ سريع، بلا 4 KPI ولا قسم "جديد اليوم من المصانع"). قسمته لنفس نمط Desktop/Mobile/switcher + useClientDashboardData.ts مشترك. الـ4 KPI والريلز وقائمة الطلبات كلها بيانات حقيقية مشتقة (لا أرقام ملفّقة) — راجع تفاصيل الحساب تحت T4.1 أعلاه. ClientDashboardMobile.tsx حالياً نقل حرفي للملف القديم (بديل مؤقت) — إعادة بنائه الفعلية هي T4.2. لا يوجد backend/DB في هذه البيئة (بلا docker) فتعذّر تسجيل دخول حي؛ تحقّق بصري عبر Playwright screenshot لـimporter-home-desktop/mobile.html + فحص curl لتحويل Vite بلا أخطاء. 101/101 اختبار ناجح، tsc نظيف. |
| 2026-07-05 | T4.2 | أعدت بناء ClientDashboardMobile.tsx بالكامل مطابقاً لـimporter-home-mobile.html (نفس نهج T3.2 لـAgentDashboardMobile): 3 KPI فقط (لا 4)، شبكة ريلز grid-cols-3، قائمة طلبات بنفس منطق الديسكتوب. استبدل النقل الحرفي المؤقت من T4.1. **نهاية المرحلة 4.** لا يوجد backend/DB بهذه البيئة فتحقّق بصري عبر Playwright screenshot + فحص curl لتحويل Vite. 101/101 اختبار ناجح، tsc نظيف. |
| 2026-07-05 | T5.1 | اكتُشف تعارض جوهري بين المرجع البصري (حاسبة بسيطة منتج واحد بحقول يدوية) والصفحة الحقيقية الموجودة `/agent/calculator` (تدفق RFQ متعدد المنتجات + محرك تسعير حقيقي بالباك إند: HS codes/VAT/formula rules/إنشاء عرض سعر فعلي، تستخدمه QuoteBuilderPage أيضاً). استشرت المستخدم — اختار الحفاظ على المنطق الحقيقي وإعادة التصميم البصري فقط (تفاصيل القرار والتنفيذ تحت T5.1 أعلاه). قسمت PricingCalcPage.tsx (ملف responsive قديم) لـDesktop/Mobile/switcher بنفس نمط كل الصفحات السابقة، مع مكوّنات مشتركة جديدة (PricingResultCard، ProductsInputTable، PricingDetailBreakdown) ووسّعت LineRow.value لـReactNode. PricingCalcPageMobile.tsx حالياً نقل حرفي مؤقت (بديل لحين T5.2). 105/105 اختبار ناجح (`--no-file-parallelism`)، tsc نظيف، dev server نظيف. |

